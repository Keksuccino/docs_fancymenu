---
title: カスタムGUI
description: 新しいGUI画面を作成および設定します。
---

# カスタムGUI

カスタムGUIは、FancyMenuの[要素](./elements)を配置できる新しい画面です。

> [!CAUTION]
> カスタムGUIはアクションを実行できます。信頼できるソースからのみインポートしてください。

# カスタムGUIの作成

1. **カスタマイズ -> カスタムGUI -> カスタムGUIの管理** を開きます。
2. **新しいGUI** を選択します。
3. 識別子を入力し、画面設定を構成します。
4. **完了** を選択し、マネージャーから新しいGUIを開きます。
5. 他の画面と同様にレイアウトを作成・編集します。

識別子には小文字、数字、`.`、`_`、`-` を使用できます。スペースは含められず、必ず一意である必要があります。空、無効、または重複した識別子は保存できません。

カスタムGUIでは常に画面カスタマイズが有効です。カスタマイズのトグルを無効にすることはできません。

# 画面設定

| 設定 | 動作 |
|---|---|
| ESCを許可 | EscapeキーでGUIを閉じ、親画面に戻れるようにします |
| ゲーム/ワールドを一時停止 | GUIを開いている間、シングルプレイを一時停止します |
| ワールド背景を描画 | GUIの背後に読み込まれたワールドを表示します |
| ワールド背景オーバーレイ | ワールド全体に標準のぼかし/暗転オーバーレイを追加します |
| ポップアップモード | カスタムGUIの背後に親画面を表示したままにします |
| ポップアップ背景オーバーレイ | ポップアップモードで親画面にぼかし/色合いを追加します |

ポップアップモードでは2つの画面は統合されません。カスタムGUIはアクティブな画面のままで、親画面はその背後に描画されます。カスタムGUIを閉じると、親画面が存在する場合はそこへ戻ります。

# カスタムGUIを開く

次のいずれかで、正確なカスタムGUI識別子を使用します。

- [**画面またはカスタムGUIを開く** アクション](./action-scripts#open-screen-or-custom-gui-opengui)
- [`/openguiscreen` コマンド](./commands#openguiscreen)

# 既存画面の上書き

カスタムGUIは、バニラまたはMODの画面が開くたびにそれを置き換えることができます。

1. 置き換え用のカスタムGUIを作成します。
2. 置き換えたい画面を開きます。
3. **カスタマイズ -> 設定 -> 詳細カスタマイズモード** を有効にします。
4. **カスタマイズ -> カスタムGUI -> 現在の画面をカスタムGUIで上書き** を選択します。
5. 置き換え用のカスタムGUIを選びます。

保存済みの上書きは **カスタマイズ -> カスタムGUI -> 上書き済み画面の管理** から管理できます。

上書きすると元の画面はスキップされるため、ナビゲーションや元の画面の動作に依存する機能を必ずテストしてください。
