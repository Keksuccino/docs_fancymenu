---
title: プレイヤーエンティティ
description: FancyMenu の「プレイヤーエンティティ」要素の仕組みと、正しく使う方法。
---

# プレイヤーエンティティ

FancyMenu では、画面にプレイヤーエンティティを追加できるため、クライアントプレイヤーや他のプレイヤー、さらには実在のプレイヤーを表さないカスタムエンティティまで、カスタムスキン・名前・ケープなど付きで表示できます。

# クライアントプレイヤー

クライアントプレイヤーの「鏡像」を表示するには、Player Entity 要素を右クリックして **Copy Client Player** を有効にするだけです。これにより、クライアントプレイヤーのスキン、ケープ、名前がコピーされます。

# 他のプレイヤー

既存の別のプレイヤーを表示したい場合は、要素を右クリックして **Player Name** を既存のプレイヤー名に設定するだけです。これにより、その既存プレイヤーのスキン、ケープ、名前が自動的に表示されます。ただし、カスタムスキンまたはカスタムケープを設定しておらず、かつ **Copy Client Player** が **無効** の場合に限ります。

# カスタムプレイヤーエンティティ

実在のプレイヤーを表示したくなくて、代わりにエンティティのスキン、ケープ、名前を完全にカスタマイズしたい場合は、要素を右クリックしてください。カスタムスキンとケープのテクスチャを設定するオプションがあります。カスタムスキンとケープが有効になっている場合、プレイヤーが存在していても、そのプレイヤー名のスキンをコピーすることなく、任意のプレイヤー名を設定することもできます。

# エンティティのポーズ

Player Entity 要素はポーズのカスタマイズを完全にサポートしているため、言い換えると、腕、脚、胴体などの各部位を自由に動かせます。

そのためには、要素を右クリックして **Player Pose** をクリックします。すると、すべての体の部位の X/Y/Z 回転を設定するためのスライダー画面が開きます。

プレイヤーポーズ設定には、スライダーで回転を調整できる通常モードと、すべての回転をテキスト入力で設定できる高度なモードがあります。高度なモードは完全なプレースホルダー対応を備えているため、回転変数を設定する Ticker 要素を使ってエンティティをアニメーションさせることも可能です！

# エンティティサイズの変更

Minecraft 1.21.1 以降では、要素の通常のリサイズ用グラバーを使って、エンティティのサイズを変更できます。

古いバージョン（1.21.0 以前）では、Player Entity 要素はリサイズ用グラバーによる直接のサイズ変更をサポートしていません。代わりに、要素を右クリックして **Scale** をクリックしてください。これにより、要素のスケールを設定できます。デフォルトは `30` で、たとえば `60` にするとプレイヤーは通常の 2 倍の大きさになり、`15` にすると半分のサイズで表示されます。

# 依存関係: Fancy Entity Renderer (FER)

Minecraft 1.21.1 以降では、Player Entity 要素を動作させるために追加の mod が必要です。その mod は「Fancy Entity Renderer」と呼ばれ、CurseForge と Modrinth で入手できます。

使用している Minecraft のバージョン向けのビルドがまだ公開されていない場合は、後ほどリリースされる可能性が高いです。

なお、FER は Keksuccino によって開発されているわけではないため、いつビルドが公開されるかについては Keksuccino に制御権がありません。
