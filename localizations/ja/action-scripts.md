---
title: アクションスクリプト
description: ボタン、スライダー、ティッカーなどでアクションスクリプトを使う方法。
---

# アクションスクリプト

FancyMenu では、要素に **アクション** を割り当てることで、メニューにインタラクティブ性を追加できます。これらのアクションは、ボタンがクリックされたとき、ティッカーが動作しているとき、スライダーが使われたとき、または画面が開閉されたときに実行されます。さらに、**if**、**else-if**、**else**、**while** といったシンプルな制御文を使って高度なアクションスクリプトを作成し、どのアクションをいつ実行するかを制御することもできます。

<img src="https://github.com/Keksuccino/FancyMenu/blob/master/assets/docs/action_script_editor.png?raw=true" alt="Action script editor" style="max-width:800px;width:100%;height:auto;">

# アクションとは？

**アクション** とは、FancyMenu がトリガーされたときに実行するタスクや処理のことです。たとえば、新しい画面を開く、チャットメッセージを送信する、音声要素の音量を調整する、といったことができます。FancyMenu のエディタでは、必要に応じて値を設定してアクションを構成します。この値には、URL やサーバーアドレスなどの追加情報を含められます。

# ステートメントとは？

より複雑な動作を作るために、FancyMenu はアクションスクリプト内で基本的な制御文をサポートしています。これには次のものが含まれます。

- **If ステートメント:** 指定した [条件](/en/conditions) が満たされた場合にのみ、アクションのブロックを実行します。
- **Else-If ステートメント:** 直前の *if*（またはそれ以前の *else-if*）が満たされなかった場合に、別の [条件](/en/conditions) を確認します。
- **Else ステートメント:** それ以前の [条件](/en/conditions) がどれも満たされなかった場合に実行されます。
- **While ステートメント:** [条件](/en/conditions) が true の間、アクションのブロックを継続的に繰り返します（無限ループを防ぐための組み込みタイムアウトがあります）。
- **Delay Block:** 指定した時間だけ待ってから、含まれているアクションを実行します。遅延のカウント中もスクリプトの他の部分は実行され続けます。
- **Execute Later Block:** 含まれているアクションをキューに入れ、ミリ秒単位の遅延後にメインスレッドで実行します。
- **Comment:** 整理のためにスクリプト内にメモを追加します。コメントはアクションを実行しません。

これらのステートメントとアクションを組み合わせることで、動的で条件付きの動作を構築できます。たとえば、警告メッセージを送る前にプレイヤーの体力が低いか確認したり、条件が変わるまで更新を繰り返したりできます。

# アクションスクリプトはどこで使える？

アクションスクリプトは用途が広く、レイアウト全体のさまざまな場所で使えます。たとえば、次のような要素に割り当てられます。

- **ボタン:** ボタンがクリックされたときにアクションを実行します。
- **ティッカー:** レイアウト内の画面表示情報を更新するために、アクションスクリプトを継続的に実行します。
- **スライダー:** スライダーの値が変わるたびにアクションスクリプトをトリガーします。
- **画面イベント:** 画面が開いたり閉じたりしたときにスクリプトを実行します（たとえば、メニュー表示時に音を鳴らすなど）。
- **リスナー:** 特定のイベントを監視するリスナーが発火すると、そのアクションスクリプトを実行します。
- **スケジューラー:** 画面が開いていなくても、時間ベースでアクションを実行します。

# アクションでプレースホルダーを使う

アクションの値は、**プレースホルダー** によって動的な内容を扱えます。ほとんどの場合、これらのプレースホルダーは JSON 風の構文を使い、アクション実行時にライブデータへ置き換えられます。

## JSON 風プレースホルダー

これらは、レイアウト内の多くの場所で使用できる通常の [プレースホルダー](/en/placeholders) です。

構文は次のとおりです。

```json
{"placeholder": "placeholder_id", "values": {"key1": "value1", "key2": "value2"}}
```

プレイヤー名、画面サイズ、**Calculator** プレースホルダーを使った計算結果など、ゲームデータを取得できます。さらに、より高度な用途のためにプレースホルダーを入れ子にすることもできます。

## `$$` プレースホルダー（変数）

`$$` プレースホルダーは特別です。FancyMenu の一部機能では、ネストされたアクション、要件、通常のプレースホルダー用にこれらの特別なプレースホルダーが提供されるため、その中で使って、要素、リスナーなどの環境に関する追加情報を取得できます。

たとえば、スライダー内でアクションを使用する場合、アクション内の `$$value` はスライダーの現在値に置き換えられます。

リスナー内でアクションを使う場合、各リスナーは押されたマウスボタン、入力された構造など、そのリスナーに関する詳細情報を取得するための独自の変数/プレースホルダーを提供します。

# アクションの設定と編集方法

要素のアクション（およびステートメントブロック）を追加・編集・削除するには、**要素を右クリック** して（ボタン、スライダー、ティッカー、その他のインタラクティブ要素を問わず）、**Manage Action Script** を選択します。すると Manage Actions 画面が開き、そこで次の操作ができます。

- **新しいアクションやステートメントを追加:** 新しいアクション項目や制御文（if、else-if、else、while）を挿入してスクリプトを作成できます。
- **既存のアクションやステートメントを編集:** アクションの値や制御ロジックを変更できます。
- **アクションやステートメントを削除:** 不要なアクションをスクリプトから削除できます。

[リスナー](/listeners) には、リスナーの管理・作成用の特別なメニューがあり、たとえばボタンやスライダーのアクションスクリプトを編集するときと同じように、そのアクションスクリプトへアクセスできます。

> Action Script Editor 画面では、大きな濃いグレーの領域を右クリックするだけで、アクション、ステートメントなどを追加するためのコンテキストメニューを開けます。
{.is-info}


# アクションスクリプトエディタのショートカットなど

アクションスクリプトエディタには、スクリプト編集をとても簡単にする便利な QoL 機能があります。

## ショートカット

- `DEL` : 選択中の項目を素早く削除
- `ENTER` : 選択中の項目のインライン編集を開始（選択中の項目にインライン編集がない場合は編集画面を開く）
- `CTRL + C` : 選択中のアクションをコピー（現時点ではアクションにのみ対応）
- `CTRL + V` : 先ほどコピーしたアクションを貼り付け
- `CTRL + Z` : 一つ戻る（元に戻す）
- `CTRL + Y` : 一つ進む（やり直し）
- `ARROW UP` : 現在選択している項目から一つ上へ移動
- `ARROW DOWN` : 現在選択している項目から一つ下へ移動
- `SHIFT + ARROW UP` : 選択中の項目を一つ上へ移動
- `SHIFT + ARROW DOWN` : 選択中の項目を一つ下へ移動
- `A` : Action Chooser 画面を素早く開いて新しいアクションを追加
- `CTRL + S` : エディタ画面を完了/保存

## さらなる QoL 機能

- アクションの値をダブルクリックすると、完全な値編集画面に入らずに値を編集できます。
- IF ステートメントの連鎖（後続の ELSE/ELSE-IF ステートメントを含む）、WHILE ループ、フォルダーは折りたたみ可能です（見た目のみで、スクリプトのロジックには影響しません）。
- エディタは常に、選択中の項目の下（または選択中のチェーン/ループ/フォルダー内）に新しいアクションを追加します。
- 濃いグレーのスクリプト領域の背景を右クリックすると、アクション、ステートメント、その他重要な項目を追加するためのコンテキストメニューが開きます。

# アクションの詳細

この一覧には、FancyMenu で利用できるアクションの大半、あるいはすべてが含まれています。更新によっては、一覧が少し古くなる場合があります。

## 次のトラック (`audio_next_track`)
- **説明:** 音声要素の次のトラックへ移動します
- **必要な値:** はい - `audio_element_identifier`（制御する音声要素の ID）

## 前のトラック (`audio_previous_track`)
- **説明:** 音声要素の前のトラックへ移動します
- **必要な値:** はい - `audio_element_identifier`（制御する音声要素の ID）

## トラック音量を設定 (`set_audio_element_volume`)
- **説明:** 音声要素の音量を設定します（0.0 から 1.0）
- **必要な値:** はい - `element_identifier:volume`

## トラックの再生/一時停止を切り替え (`audio_toggle_play`)
- **説明:** 音声要素の現在のトラックの再生/一時停止を切り替えます
- **必要な値:** はい - `audio_element_identifier`

## 音声を再生 (`play_audio`)
- **説明:** 音声リソースを一度再生します。このアクションは開始した音声を追跡し、後で `stop_all_action_audios` によって停止できるようにします。
- **必要な値:** はい - `audioSource`、`soundChannel`、`baseVolume` を含む JSON 設定
- **値の例:** `{"audioSource":"[source:local]/config/fancymenu/assets/example.ogg","soundChannel":"master","baseVolume":1.0}`

## すべてのアクション音声を停止 (`stop_all_action_audios`)
- **説明:** **音声を再生** アクションによって開始されたすべての音声トラックを停止します。音声要素、メニューの開閉音、ボタン音、その他の音声システムは停止しません。
- **必要な値:** いいえ

## ビデオ要素の音量を設定 (`set_video_element_volume`)
- **説明:** ビデオ要素の音量を設定します（0.0 から 1.0）
- **必要な値:** はい - `video_element_identifier:volume`

## ビデオ要素の再生位置を設定 (`set_video_element_play_time`)
- **説明:** ビデオ要素をミリ秒単位のタイムスタンプへシークします
- **必要な値:** はい - `video_element_identifier:timestamp_ms`

## ビデオ要素の一時停止状態を切り替え (`toggle_video_element_pause_state`)
- **説明:** ビデオ要素の一時停止状態を切り替えます
- **必要な値:** はい - `video_element_identifier`

## ビデオ背景の音量を設定 (`set_video_menu_background_volume`)
- **説明:** ビデオメニュー背景の音量を設定します（0.0 から 1.0）
- **必要な値:** はい - `background_identifier:volume`

> 背景の識別子を取得するには、エディタ背景を右クリックして 'Copy Background Identifier' をクリックしてください。
{.is-info}

## ビデオ背景の再生位置を設定 (`set_video_menu_background_play_time`)
- **説明:** ビデオメニュー背景をミリ秒単位のタイムスタンプへシークします
- **必要な値:** はい - `background_identifier:timestamp_ms`

> 背景の識別子を取得するには、エディタ背景を右クリックして 'Copy Background Identifier' をクリックしてください。
{.is-info}

## ビデオ背景の一時停止状態を切り替え (`toggle_video_menu_background_pause_state`)
- **説明:** ビデオメニュー背景の一時停止状態を切り替えます
- **必要な値:** はい - `background_identifier`

> 背景の識別子を取得するには、エディタ背景を右クリックして 'Copy Background Identifier' をクリックしてください。
{.is-info}

## レイアウトを切り替え (`toggle_layout`)
- **説明:** 名前でレイアウトの有効/無効を切り替えます
- **必要な値:** はい - `layout_name`

## レイアウトを有効化 (`enable_layout`)
- **説明:** 名前でレイアウトを有効化します
- **必要な値:** はい - `layout_name`

## レイアウトを無効化 (`disable_layout`)
- **説明:** 名前でレイアウトを無効化します
- **必要な値:** はい - `layout_name`

## 画面またはカスタム GUI を開く (`opengui`)
- **説明:** 識別子によって画面を開きます（バニラ、MOD、またはカスタム GUI）
- **必要な値:** はい - `screen_identifier`

> このアクションは**すべての画面で動作するわけではありません**。特に MOD の画面ではそうです。画面を開けなかった場合はエラーが表示されます。その場合にできることはあまりありません。おそらく、その画面は FancyMenu で自動的に開くには複雑すぎるためです。
> 
> MOD 画面への互換性は、今後 FancyMenu 側で手動追加されることもありません。存在するすべての MOD に対応するには膨大な時間がかかるためです。申し訳ありません。そのような場合、相手の MOD の開発者に連絡することも通常はおすすめしません。FancyMenu が画面を開けないのであれば、簡単に対応を追加できる方法がないからです。推奨される回避策は、特定の画面を開くボタンを模倣する **"Mimic Vanilla/Mod Button"** アクションを試すことです。ボタンがない場合は、残念ながらどうしようもありません。
{.is-info}

## 画面を閉じる (`closegui`)
- **説明:** アクティブな画面を閉じます
- **必要な値:** いいえ

## 画面を更新 (`update_screen`)
- **説明:** 現在の画面を再初期化します
- **必要な値:** いいえ

## 前の画面に戻る (`back_to_last_screen`)
- **説明:** 前の画面（現在の画面の前にあった画面）に戻ります
- **必要な値:** いいえ

## サーバーに参加 (`joinserver`)
- **説明:** プレイヤーを Minecraft サーバーに接続します
- **必要な値:** はい - `server_ip:port`

## ワールドに入る (`loadworld`)
- **説明:** Minecraft ワールドに入ります
- **必要な値:** はい - `world_folder_name`

## 前回のワールド/サーバーに入る/参加する (`join_last_world`)
- **説明:** プレイヤーが最後にいたワールドまたはサーバーに入る/参加します
- **必要な値:** いいえ

## ワールドまたはサーバーから退出 (`disconnect_server_or_world`)
- **説明:** ワールドまたはサーバーから退出し、指定した画面を開きます
- **必要な値:** はい - `screen_identifier`

## Minecraft を終了 (`quitgame`)
- **説明:** Minecraft を完全に終了します
- **必要な値:** いいえ

## チャットメッセージ/コマンドを送信 (`sendmessage`)
- **説明:** チャットメッセージを送信するか、チャットコマンドを実行します
- **必要な値:** はい - `message_text` または `/command_text`

## 統合サーバーとしてコマンドを実行 (`execute_command_as_integrated_server`)
- **説明:** シングルプレイで統合サーバーとしてコマンドを強制実行し、権限とチート設定を無視します。
- **必要な値:** はい - コマンドテキスト。例: `/give @p minecraft:diamond 1`

> このアクションは、シングルプレイでワールドが LAN 公開されていない場合にのみ動作します。マルチプレイヤーサーバーでは意図的に何もしません。
{.is-warning}

## チャットに貼り付け (`paste_to_chat`)
- **説明:** チャット入力欄にテキストを貼り付けます（追記または置換）
- **必要な値:** はい - `true:Text` または `false:Text`

## チャットに表示 [クライアント側] (`display_in_chat_client_side`)
- **説明:** テキストをローカルチャットに直接出力します（サーバーは不要）
- **必要な値:** はい - `text_or_json`

## FM データをサーバーへ送信 (`send_fm_data_to_server`)
- **説明:** FM Data パケットチャネルを通じて、カスタムテキストデータを現在の FancyMenu サーバーへ送信します。
- **必要な値:** はい - `data_identifier||data`

## リモートサーバーへ接続 (`connect_to_remote_server`)
- **説明:** クライアント開始の WebSocket 接続を外部のリモートサーバーへ開くか、既存の接続を再利用します。
- **必要な値:** はい - リモートサーバー URL。例: `wss://example.com/ws`

## リモートサーバーへデータ送信 (`send_data_to_remote_server`)
- **説明:** リモートサーバー接続を開くか再利用し、テキストデータを送信します。
- **必要な値:** はい - `remote_server_url||data`

## リモートサーバー接続を閉じる (`close_remote_server_connection`)
- **説明:** リクエスト ID によって特定のリモートサーバー接続を閉じます。
- **必要な値:** はい - リクエスト ID。通常は `$$request_id` のような Remote Server リスナー変数から取得します

## すべてのリモートサーバー接続を閉じる (`close_all_remote_server_connections`)
- **説明:** FancyMenu によって開かれたすべてのアクティブなリモートサーバー接続を閉じます。
- **必要な値:** いいえ

## ブラウザで URL を開く (`openlink`)
- **説明:** デフォルトのブラウザでリンクを開きます
- **必要な値:** はい - `https://example.com`

## テキストをクリップボードにコピー (`copytoclipboard`)
- **説明:** テキストをクリップボードにコピーします
- **必要な値:** はい - `text_to_copy`

## ゲームログに出力 (`print_to_log`)
- **説明:** ゲームログに1行書き込みます
- **必要な値:** はい - `text_to_log`

## 変数の値を設定（FM 変数） (`set_variable`)
- **説明:** FancyMenu の変数にテキスト内容を保存します
- **必要な値:** はい - `variable_name:variable_value`

## すべての変数をクリア（FM 変数） (`clear_variables`)
- **説明:** FancyMenu に保存されている**すべて**の変数を消去します
- **必要な値:** いいえ

## HTTP リクエストを送信 (`send_http_request`)
- **説明:** HTTP リクエストを送信します。レスポンスを変数に保存することもできます
- **必要な値:** はい - HTTP リクエスト設定

> このアクションを使うと、REST API、webhook、その他の HTTP エンドポイントへデータを送信できます。
> さまざまな認証方式、カスタムヘッダー、異なるリクエスト種別に対応しています。
> 
> また、このアクションでは、後で使うためにリクエストのレスポンスを FancyMenu の変数に保存することもできます！
{.is-info}

## リソースパックを管理 (`manage_resource_pack`)
- **説明:** 表示名でリソースパックを有効化/無効化/切り替えします（必要に応じて再読み込み）
- **必要な値:** はい - `pack_name|||MODE|||reload_bool`

## リソースパックを再読み込み (`reload_resource_packs`)
- **説明:** リソースパックを再読み込みします（5秒クールダウン）
- **必要な値:** いいえ

## FancyMenu を再読み込み (`reloadmenu`)
- **説明:** パノラマ、スライドショー、すべてのリソースを含めて FancyMenu を再読み込みします（負荷大）
- **必要な値:** いいえ

> このアクションは**パフォーマンスに大きな影響**を与え、ティッカーで使用するとラグの原因になる可能性があります。ボタン以外での使用はおすすめしません。
{.is-warning}

## 要素アニメーターを切り替え (`toggle_element_animator`)
- **説明:** 要素アニメーターの再生状態を切り替えます
- **必要な値:** はい - `animator_identifier`

## 要素アニメーターを有効化 (`enable_element_animator`)
- **説明:** 要素アニメーターを有効化します
- **必要な値:** はい - `animator_identifier`

## 要素アニメーターを無効化 (`disable_element_animator`)
- **説明:** 要素アニメーターを無効化します
- **必要な値:** はい - `animator_identifier`

## 要素アニメーターをリセット (`reset_element_animator`)
- **説明:** 要素アニメーターのタイムライン/状態をリセットします
- **必要な値:** はい - `animator_identifier`

## バニラ/Mod ボタンを模倣 (`mimicbutton`)
- **説明:** バニラまたは MOD のボタンのクリック動作を模倣します
- **必要な値:** はい - `screen_identifier:widget_locator`

## キーバインドを模倣 (`mimic_keybind`)
- **説明:** Minecraft のキーバインドを実行します（必要に応じて押し続け可）
- **必要な値:** はい - `keybind_id|||keep_pressed_bool|||duration_ms`

## テキスト入力欄の値を設定 (`set_text_input_field_value`)
- **説明:** カスタムまたはバニラの入力欄の値を要素識別子で設定します。
- **必要な値:** はい - `element_identifier|||new_value|||force_set_when_inactive`

## ゲームディレクトリにファイルを作成 (`create_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）に空のファイルを作成します。`.minecraft/` プレフィックスにも対応しており、デフォルトのランチャープロファイルディレクトリを対象にできます（現在のインスタンスディレクトリとは異なる場合があります）。
- **必要な値:** はい - `file_path`

## ゲームディレクトリ内のファイル/フォルダーを削除 (`delete_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）内のファイルまたはフォルダーを削除します。`.minecraft/` プレフィックスでデフォルトのランチャープロファイルを対象にできます（実行中のインスタンスとは異なる場合があります）。フォルダー内の**直下のすべてのファイル**を削除するには、パスの末尾に `*` を付けます（サブディレクトリは無視し、フォルダー自体は残します）。
- **必要な値:** はい - `target_path`

## ゲームディレクトリ内のファイル/フォルダーをコピー (`copy_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）内でコピーします。`.minecraft/` プレフィックスはデフォルトのランチャープロファイルを対象にします（現在のインスタンスとは限りません）。**元の**パスの末尾に `*` を付けると、そのフォルダー内の直下にあるすべてのファイルをコピーできます（サブディレクトリは無視）。コピー先はディレクトリである必要があり、`*` は使えません。
- **必要な値:** はい - `source||destination`

## ゲームディレクトリ内のファイル/フォルダーを移動 (`move_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）内で移動します。`.minecraft/` プレフィックスはデフォルトのランチャープロファイルを対象にします（現在のインスタンスと異なる場合があります）。**元の**パスの末尾に `*` を付けると、そのフォルダー内の直下にあるすべてのファイルを移動できます（サブディレクトリは無視）。移動先はディレクトリである必要があり、`*` は使えません。
- **必要な値:** はい - `source||destination`

## ゲームディレクトリ内のファイル/フォルダー名を変更 (`rename_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）内のファイルまたはフォルダーの名前を変更します。`.minecraft/` プレフィックスはデフォルトのランチャープロファイルを対象にします（現在のインスタンスと異なる場合があります）。中身はそのままで、名前だけが変わります。
- **必要な値:** はい - `path||new_name`

## ゲームディレクトリにファイルをダウンロード (`download_file_to_game_dir`)
- **説明:** ファイルを非同期でゲームディレクトリ（インスタンスのルート）へダウンロードします。`.minecraft/` プレフィックスはデフォルトのランチャープロファイルを対象にします（必ずしも実行中のインスタンスではありません）。**対象フォルダー**を指定してください。ファイル名はヘッダー/URL から自動的に決定されます。
- **必要な値:** はい - `url||target_folder`

## ゲームディレクトリ内の ZIP ファイルを展開 (`extract_zip_file_in_game_dir`)
- **説明:** ZIP ファイルをゲームディレクトリまたはデフォルトの `.minecraft` ディレクトリ内の対象フォルダーへ展開します。完了すると **On ZIP Extracted via Action** リスナーを発火します。
- **必要な値:** はい - `source_zip_path||target_folder_path`

## ゲームディレクトリ内でファイル/フォルダーを開く (`open_file_folder_in_game_dir`)
- **説明:** ファイルまたはフォルダーを OS の既定アプリで開きます。安全上の理由から、対象はゲームディレクトリまたはデフォルトの `.minecraft` ディレクトリ内に留まっている必要があります。
- **必要な値:** はい - `target_path`

## ゲームディレクトリにファイルを書き込む (`write_file_in_game_dir`)
- **説明:** ゲームディレクトリ（インスタンスのルート）内にテキストを書き込むか追記します。`.minecraft/` プレフィックスはデフォルトのランチャープロファイルを対象にします（このインスタンスとは異なる場合があります）。ファイルが存在しない場合は作成します。値内の `\n` で改行を挿入できます。追記モードは最後のブール値で制御されます。
- **必要な値:** はい - `path|||content|||append_bool`

## システムからファイルを選択 (`select_file_to_game_dir`)
- **説明:** ネイティブのファイルピッカー（任意の場所）を開き、選択したファイルをゲームディレクトリ（インスタンスのルート）へコピーするか、`.minecraft/` が付いている場合はデフォルトの `.minecraft/` へコピーします（そのデフォルトはこのインスタンスと異なる場合があります）。拡張子フィルター、カスタムフィルターラベル、上書き切り替えオプションに対応しています。
- **必要な値:** はい - 選択設定

## トーストを表示 (`show_toast`)
- **説明:** 設定可能なトースト通知を表示します
- **必要な値:** はい - トースト設定

## スケジューラーを開始 (`start_scheduler`)
- **説明:** スケジューラー ID でスケジューラーを開始します。
- **必要な値:** はい - `scheduler_id`

## スケジューラーを停止 (`stop_scheduler`)
- **説明:** スケジューラー ID でスケジューラーを停止します。
- **必要な値:** はい - `scheduler_id`

## Minecraft のオプションを設定 (`edit_minecraft_option`)
- **説明:** Minecraft の設定オプションを編集します
- **必要な値:** はい - `option_name:set_to_value`
