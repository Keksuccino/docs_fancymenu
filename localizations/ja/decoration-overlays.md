---
title: 装飾オーバーレイ
description: FancyMenu のレイアウトエディタで、メニューにフルスクリーンのビジュアルオーバーレイを追加します。
---

# 装飾オーバーレイ

装飾オーバーレイは、メニュー要素の前面に表示されるフルスクリーン効果です。

これらは、手動で効果を作り込まずに、メニューへ雰囲気や動きを追加したいときに便利です。

代表的な例:

- **Snowfall** を追加して、雪が積もる冬向けメニューにする。
- **Rainfall** を追加して、水たまりや滴のある嵐のような見た目にする。
- **Fireflies** を追加して、落ち着いた夜風のメニューにする。
- **String Lights** を追加して、季節感や装飾的なテーマにする。
- **Leaves**、**Fireworks**、または **Confetti** を追加して、季節イベント用のメニューにする。
- **Browser** オーバーレイを追加して、フルスクリーンの Web ページ/動画レイヤーを表示する。

# 場所

レイアウトエディタでレイアウトを開き、エディタ背景を右クリックして **Decoration Overlays** を開きます。

# クイックスタート

1. レイアウトエディタでレイアウトを開きます。
2. 背景の空白部分を右クリックします。
3. **Decoration Overlays** を開きます。
4. オーバーレイの種類を選択します。
5. **Show Overlay** を **Enabled** にします。
6. オーバーレイ設定を構成します。
7. レイアウトを保存して画面をテストします。

# オーバーレイの種類の仕組み

各オーバーレイ種類には、それぞれ専用のサブメニューと独自の **Show Overlay** トグルがあります。

- 必要な種類だけを有効化できます。
- 1 つのレイアウトで、複数の有効な種類を組み合わせられます。
- 設定はオーバーレイ種類ごとに分かれています（例: 色、強さ、速度、密度、スケール、特殊な挙動）。

> [!INFO]
> 同じ種類を有効にした複数のレイアウトを使うことで、同じオーバーレイ種類を複数重ねることができます。

# オーバーレイの種類

- **Snowfall**: 必要に応じて表面やボタン上に雪を積もらせることができる降雪。
- **Rainfall**: 水たまり、滴、任意の雷の閃光を追加できる雨。
- **Fireflies**: グループ数、密度、サイズ、色を設定できる、動くホタルの समूह。
- **String Lights**: 設定可能な電飾の組み合わせ、ライト色、風/ちらつきの挙動、ホリデー配色モード。
- **Leaves**: 色、風、速度、スケール、密度を設定できる落ち葉。
- **Fireworks**: 発生頻度の高い花火で、数、爆発サイズ、スケールを設定可能。
- **Confetti**: マウスクリックの紙吹雪モードを追加できる紙吹雪の雨。
- **Browser**: URL とメディア設定を備えたフルスクリーンのブラウザオーバーレイ。
- **GLSL Shader**: フルスクリーンのカスタムシェーダーオーバーレイ（アニメーションまたは静的なシェーダーベースの演出用）。

# Browser オーバーレイ: インタラクティブとパッシブ

Browser オーバーレイは、インタラクティブなブラウザとしても、受動的なビジュアルレイヤーとしても構成できます。

- **Process Mouse/Keyboard** 設定は、ブラウザ自体が入力を処理するかどうかを制御します。
- **Consume Mouse/Keyboard** 設定は、背後のメニューに入力が届かないようにするかどうかを制御します。

実用的な設定例:

- 前面で操作するブラウザ: **Process** と **Consume** の両方を有効にする。
- 表示専用のブラウザレイヤー: **Process** を無効にし、**Consume** も無効にする。

> [!IMPORTANT]
> Browser 装飾オーバーレイには **MCEF** モッドが必要です。
