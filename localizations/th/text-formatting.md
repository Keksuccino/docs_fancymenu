---
title: การจัดรูปแบบข้อความ
description: วิธีจัดรูปแบบข้อความด้วย Markdown และโค้ดจัดรูปแบบของ Minecraft
---

# การจัดรูปแบบข้อความ

FancyMenu มีฟีเจอร์มากมายที่จะทำให้ข้อความในเลย์เอาต์ดู *สวยขึ้น*!

องค์ประกอบข้อความรองรับ **Markdown** แบบเต็มรูปแบบพร้อมลูกเล่นเจ๋ง ๆ เพิ่มเติม และเนื้อหาข้อความส่วนใหญ่อื่น ๆ ก็รองรับระบบ **การจัดรูปแบบข้อความของ Minecraft** ด้วย อีกทั้งป้ายกำกับปุ่มยังรองรับ **คอมโพเนนต์ข้อความของ Minecraft** ซึ่งทำให้คุณใช้ฟอนต์กำหนดเองและอื่น ๆ ได้อีกด้วย

# Markdown

**องค์ประกอบข้อความ** ของ FancyMenu รองรับ Markdown แบบเต็มรูปแบบ ซึ่งหมายความว่าคุณสามารถจัดรูปแบบข้อความได้โดยการเพิ่มอักขระพิเศษลงไป

ตัวอย่างเช่น หากต้องการทำให้ข้อความเป็นตัวหนา ให้ใส่ `**` ไว้ก่อนและหลังข้อความที่ต้องการเป็นตัวหนา ดังนั้น `**Some bold text that's very bold.**` จะออกมาดังนี้:
**Some bold text that's very bold.**

Markdown ของ FancyMenu ยังมีฟีเจอร์พิเศษบางอย่างที่ทำให้ทรงพลังยิ่งขึ้น!

> Markdown **ใช้ไม่ได้** กับข้อความชนิดอื่น ๆ เช่นป้ายกำกับปุ่ม มันใช้ได้เฉพาะกับ **TEXT ELEMENTS** เท่านั้น สำหรับอย่างอื่นทั้งหมด โปรดใช้ [โค้ดจัดรูปแบบของ Minecraft](/text-formatting#minecraft-text-formatting)
{.is-danger}

## ฟอนต์

คุณสามารถแสดงข้อความด้วยฟอนต์กำหนดเองที่โหลดผ่าน resource pack ได้โดยใส่ `%!!<font_name>%` ไว้ก่อนข้อความ และ `%!!%` ไว้หลังข้อความ

ฟอนต์ที่ใช้ได้และมีมาในเกมพื้นฐานคือ `uniform` ดังนั้นหากต้องการแสดงข้อความด้วยฟอนต์ `uniform` ให้ทำแบบนี้:
`%!!uniform%this is a custom font%!!%`

จะทำให้ `this is a custom font` แสดงผลด้วยฟอนต์ `uniform`

## สีข้อความ (HEX)

คุณสามารถแสดงข้อความด้วยสี HEX ที่ต้องการได้ โดยใส่ `%<HEX_color>%` ไว้ก่อนข้อความ และ `%#%` ไว้หลังข้อความ

สี HEX ที่ใช้ได้สำหรับสีเขียวคือ `#77fc03` ดังนั้นหากต้องการแสดงข้อความด้วยสีนี้ ให้ทำแบบนี้:
`%#77fc03%this text is green!%#%`

จะทำให้ `this text is green!` แสดงเป็นสี `#77fc03` (สีเขียว)

ตรวจสอบให้แน่ใจว่า HEX color เริ่มต้นด้วย `#`!

FancyMenu 3.9.0 ยังรองรับชื่อสีแบบ HTML ทั่วไปในโค้ดจัดรูปแบบสีเดียวกันนี้ด้วย:

```
%#red%This text is red!%#%
```

ชื่อที่รองรับ: `black`, `silver`, `gray`, `grey`, `white`, `maroon`, `red`, `purple`, `fuchsia`, `magenta`, `green`, `lime`, `olive`, `yellow`, `navy`, `blue`, `teal`, `aqua`, `cyan`, และ `transparent`

## การจัดแนวข้อความ

คุณสามารถจัดแนวแต่ละบรรทัดได้โดยเริ่มบรรทัดด้วยโค้ดจัดแนวที่ต้องการ จากนั้นไม่มีอะไรต่อท้าย แล้วตามด้วยบรรทัดข้อความที่ต้องการแสดงด้วยการจัดแนวนั้น และใส่โค้ดจัดแนวนั้นอีกครั้งในบรรทัดว่างถัดไป

เนื้อหาข้อความทั้งหมดจะ **ชิดซ้ายเป็นค่าเริ่มต้น** ดังนั้นจึงมีโค้ดจัดรูปแบบสำหรับ **กึ่งกลาง** และ **ชิดขวา** เท่านั้น

### กึ่งกลาง

หากต้องการจัดข้อความให้อยู่กึ่งกลาง ให้ใช้โค้ดจัดรูปแบบ `^^^`

ตัวอย่าง:
```
This text is not centered.

^^^
This text is centered.
This text is also centered.
^^^

This text is not centered anymore.
```

### ชิดขวา

หากต้องการแสดงข้อความชิดขวา ให้ใช้โค้ดจัดรูปแบบ `|||
`

ตัวอย่าง:
```
This text is not right-aligned.

|||
This text is right-aligned.
This text is also right-aligned.
|||

This text is not right-aligned anymore.
```

## หัวเรื่อง

หากต้องการแสดง **ข้อความหนึ่งบรรทัด** เป็นหัวเรื่อง (ขนาดใหญ่ขึ้นและมีเส้นใต้) ให้ใส่ `# ` (ใหญ่มาก), `## ` (ใหญ่) หรือ `### ` (เล็ก) ไว้หน้าบรรทัดข้อความ

ตัวอย่าง:
`## Big Headline`

## ตัวหนา

ใส่ `**` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็น **ตัวหนา**

ตัวอย่าง:
`**bold text content**`

## ตัวเอียง

ใส่ `_` หรือ `*` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็น *ตัวเอียง*

ตัวอย่าง:
`*italic text content*`

## ขีดฆ่า

ใส่ `~` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็นข้อความ~~ขีดฆ่า~~

ตัวอย่าง:
`~strikethrough text content~`

## ไฮเปอร์ลิงก์

คุณสามารถเพิ่มลิงก์ไปยังข้อความได้ ซึ่งจะเปิดเว็บไซต์เมื่อคลิก

ข้อความที่ต้องการให้แสดงเป็น [ไฮเปอร์ลิงก์](https://google.com) ต้องครอบด้วย `[ ]` และตามด้วยลิงก์จริงที่ครอบด้วย `( )`

ดังนั้นถ้าคุณต้องการให้ `example text content` คลิกได้และเปิด `https://example-website.net` ให้ทำแบบนี้:
`[example text content](https://example-website.net)`

## เหตุการณ์คลิกและโฮเวอร์

FancyMenu 3.9.0 เพิ่มเหตุการณ์คลิกและโฮเวอร์แบบ Markdown สำหรับ Text elements และข้อความ Markdown อื่น ๆ

เหตุการณ์คลิกใช้คำนำหน้า `click:`

```
[some clickable text](click:unique_text_click_event_id)
```

เหตุการณ์โฮเวอร์ใช้คำนำหน้า `hover:`

```
[some hoverable text](hover:unique_text_hover_event_id)
```

ใช้ตัวฟัง **On Markdown Text Clicked** และ **On Markdown Text Hovered** เพื่อโต้ตอบกับเหตุการณ์เหล่านี้ ตัวฟังทั้งสองจะเปิดเผย ID ของเหตุการณ์ในตัวแปร `$$text_event_id`

## รูปภาพ

Markdown รองรับการแสดงรูปภาพในเนื้อหาข้อความ

FancyMenu รองรับทรัพยากร Minecraft, ทรัพยากรภายในเครื่อง และทรัพยากรเว็บใน Markdown

หากต้องการเพิ่มรูปภาพ ให้เริ่มบรรทัดข้อความด้วย `![](` จากนั้นใส่ [URL, ตำแหน่งทรัพยากร หรือพาธไปยังทรัพยากร](/resources) แล้วปิดด้วย `)`

ดังนั้นหากต้องการแสดงทรัพยากรเว็บ `https://example-website.net/image.png` ให้ทำแบบนี้:
`![](https://example-website.net/image.png)`

รูปภาพยังสามารถเป็น **ไฮเปอร์ลิงก์** ได้ โดยครอบบรรทัดรูปภาพทั้งหมดด้วย **ไฮเปอร์ลิงก์** แบบนี้:
`[![](https://example-website.net/image.png)](https://example-website.net)`

> ทรัพยากรภายในเครื่องต้องอยู่ใน `/config/fancymenu/assets/` !
{.is-warning}

## ข้อความอ้างอิง

หากต้องการจัดข้อความให้เป็นคำอ้างอิง ให้เริ่มบรรทัดข้อความด้วย `> `
ข้อความที่ตามมาทั้งหมดจะถูกจัดเป็นคำอ้างอิงจนกว่าจะพบบรรทัดที่ **ว่างเปล่า**

ตัวอย่าง:
```
This will not look like a quote.

> This will look like a quote.
This will also look like a quote.

This will not look like a quote anymore.
```

## รายการแบบหัวข้อย่อย

หากต้องการแสดงข้อความเป็นรายการแบบหัวข้อย่อยดังนี้:
- Entry 1
- Entry 2
  - Sub-Entry

คุณเพียงแค่เริ่มบรรทัดด้วย `- `

ตัวอย่าง:
```
- Entry 1
- Entry 2
  - Sub-Entry
```

## เส้นคั่น

หากต้องการเพิ่มเส้นคั่นในข้อความที่มีความกว้างเท่ากับหนึ่งบรรทัดข้อความ ให้เริ่มบรรทัดด้วย `---` และไม่ต้องใส่อะไรต่อท้าย

จากนั้นจะมีลักษณะประมาณนี้:

---

## บล็อกโค้ด

บล็อกโค้ดช่วยให้คุณแสดงข้อความเป็น `plain text` โดยไม่ให้ Markdown พยายามจัดรูปแบบ หรือใช้เพื่อแสดงข้อความในสไตล์คล้ายโค้ดโดยไม่ตัดบรรทัดอัตโนมัติ

บล็อกโค้ดแบบบรรทัดเดียว (อยู่ระหว่างข้อความอื่น) จะเริ่มและจบด้วย \` ซึ่งจริง ๆ แล้วค่อนข้างยากที่จะเขียนแสดงในข้อความ Markdown..

บรรทัดข้อความที่มีบล็อกโค้ดแบบบรรทัดเดียวจะมีลักษณะดังนี้:

![single_line_code_block](https://github.com/Keksuccino/FancyMenu/assets/35544624/e78fd38b-7b1a-4f00-9b11-797d964b3b43)

บล็อกโค้ดหลายบรรทัดจะครอบหลายบรรทัดไว้ในบล็อกโค้ดก้อนใหญ่เดียว และเริ่มด้วยบรรทัดที่มีเพียง \`\`\` จากนั้นตามด้วยเนื้อหาข้อความ แล้วปิดด้วย \`\`\` อีกครั้ง:

![multi_line_code_block](https://github.com/Keksuccino/FancyMenu/assets/35544624/bf8c77eb-a97e-48cd-9270-8302c2995856) 

## ข้อความธรรมดา

โค้ดจัดรูปแบบแบบ plain text จะข้ามโค้ดจัดรูปแบบอื่นทั้งหมดที่อยู่ภายใน

ทำงานคล้ายกับบล็อกโค้ด แต่จะไม่จัดรูปแบบเหมือนบล็อกโค้ด แทนที่จะเป็นเช่นนั้น มันจะแสดงเป็นข้อความปกติแต่ไม่มีการจัดรูปแบบใด ๆ

หากต้องการครอบส่วนข้อความภายในหนึ่งบรรทัดด้วยโค้ดจัดรูปแบบแบบ plain text ให้ใส่ `;;` ไว้ก่อนและหลังส่วนข้อความที่ต้องการให้แสดงเป็น plain text แบบนี้:

```
This is a line of text with ;;**this part**;; showing as unformatted text with visible ** (bold) formatting code and _this part_ as normal formatted italic text.
```

Plain text ยังใช้เป็นโค้ดครอบหลายบรรทัดได้ด้วย หากต้องการครอบทั้งบรรทัด ให้ใส่ `;;;` ไว้ก่อนและหลังบรรทัดที่ต้องการแสดงเป็น plain text แบบนี้:

```
;;;
This line will show **unformatted** with visible ** (bold) formatting codes.
This line will also show _unformatted_ with visible _ (italic) formatting codes.
;;;

This line will look **normal** again with the "normal" formatted as bold text.
```

# การจัดรูปแบบข้อความของ Minecraft

ตัว Minecraft เองมีระบบจัดรูปแบบที่ค่อนข้างดี ซึ่งทำงานคล้าย Markdown โดยคุณสามารถเพิ่มอักขระพิเศษลงในเนื้อหาข้อความเพื่อจัดรูปแบบได้

หากต้องการอ่านเพิ่มเติมเกี่ยวกับระบบจัดรูปแบบของ Minecraft โปรดดูที่ [หน้า Minecraft wiki นี้](https://minecraft.wiki/w/Formatting_codes)

> ใน wiki จะระบุว่าคำนำหน้าโค้ดจัดรูปแบบคือ `§` แต่ใน FancyMenu คุณต้องเปลี่ยนเป็น `&` อย่างอื่นยังคงเหมือนเดิม
{.is-warning}

> **องค์ประกอบข้อความ** มีความซับซ้อนมาก และเพื่อรองรับ Markdown จึงต้องแลกด้วยการ **ทำให้โค้ดจัดรูปแบบแบบ Vanilla ของ Minecraft ใช้งานไม่ได้** ดังนั้นโค้ดเหล่านี้จะทำงานไม่ดีใน Text elements (เช่น หลังโค้ดจัดรูปแบบจะมีผลเฉพาะคำแรก ฯลฯ) คุณควรใช้โค้ดจัดรูปแบบแบบ Markdown ใน Text elements แทน
{.is-danger}

# คอมโพเนนต์ข้อความของ Minecraft (ระบบ Raw Component)

ระบบคอมโพเนนต์ข้อความของ Minecraft มีความทรงพลังมากสำหรับเนื้อหาข้อความแบบ **หนึ่งบรรทัด** เช่น **ป้ายกำกับปุ่ม**

ใน Vanilla Minecraft คุณสามารถใช้ได้ในคำสั่ง `/tellraw` และ `/title` (และอาจรวมถึงที่อื่นด้วย)
มันคือข้อความที่จัดรูปแบบแล้วซึ่งถูกซีเรียลাইজเป็น JSON ดังนั้นคุณจึงสามารถเพิ่มแอตทริบิวต์การจัดรูปแบบให้กับเนื้อหาข้อความได้

หากต้องการเรียนรู้เกี่ยวกับ text components แบบละเอียด โปรดดูที่ [หน้า Minecraft wiki นี้](https://minecraft.wiki/w/Raw_JSON_text_format)
หากต้องการเรียนรู้เกี่ยวกับฟอนต์ใน Minecraft โปรดดูที่ [หน้า Minecraft wiki นี้](https://minecraft.wiki/w/Resource_pack#Fonts)

หากต้องการให้ FancyMenu ตรวจพบป้ายกำกับปุ่มเป็น **text component** ให้ใส่เฉพาะข้อความ component ที่ซีเรียลাইজแล้วเป็นป้ายกำกับ เช่นนี้:
`{"text":"Button Label Text","font":"uniform"}`

ตัวอย่างข้างต้นจะแสดงป้ายกำกับปุ่ม `Button Label Text` ด้วยฟอนต์ `uniform`

> คุณสามารถใช้ placeholder ของ FancyMenu ในค่า `text` ของ components ได้
{.is-info}
