---
title: การจัดรูปแบบข้อความ
description: วิธีจัดรูปแบบข้อความด้วย Markdown และโค้ดการจัดรูปแบบของ Minecraft
---

# การจัดรูปแบบข้อความ

[องค์ประกอบข้อความ](./elements#text) รองรับ Markdown ฟิลด์ข้อความอื่น ๆ ใช้การจัดรูปแบบของ Minecraft และป้ายกำกับปุ่มสามารถใช้คอมโพเนนต์ข้อความของ Minecraft ได้

# Markdown

**Text elements** ของ FancyMenu รองรับ Markdown แบบเต็มรูปแบบ ซึ่งหมายความว่าคุณสามารถจัดรูปแบบเนื้อหาข้อความได้ด้วยการเพิ่มอักขระพิเศษลงไป

ตัวอย่างเช่น หากต้องการทำให้ข้อความเป็นตัวหนา ให้ใส่ `**` ไว้ก่อนและหลังข้อความที่เป็นตัวหนา ดังนั้น `**Some bold text that's very bold.**` จะมีลักษณะดังนี้:
**Some bold text that's very bold.**

FancyMenu ยังรองรับส่วนขยายที่อธิบายไว้ด้านล่างด้วย

> [!CAUTION]
> Markdown ใช้ได้เฉพาะใน **Text elements** เท่านั้น สำหรับป้ายกำกับปุ่มและฟิลด์ข้อความอื่น ๆ ให้ใช้ [รหัสการจัดรูปแบบของ Minecraft](#minecraft-text-formatting)

## ฟอนต์

คุณสามารถแสดงข้อความด้วยฟอนต์แบบกำหนดเองที่โหลดผ่าน resource pack ได้ โดยใส่ `%!!<font_name>%` ไว้ก่อนข้อความ และ `%!!%` ไว้หลังข้อความ

ฟอนต์ที่ใช้งานได้และมีอยู่ในเกมพื้นฐานคือ `uniform` ดังนั้นหากต้องการแสดงข้อความด้วยฟอนต์ `uniform` ให้ทำดังนี้:
`%!!uniform%this is a custom font%!!%`

สิ่งนี้จะแสดง `this is a custom font` ด้วยฟอนต์ `uniform`

## สีข้อความ (HEX)

คุณสามารถแสดงข้อความด้วยสี HEX ที่ต้องการได้ โดยใส่ `%<HEX_color>%` ไว้ก่อนข้อความ และ `%#%` ไว้หลังข้อความ

สี HEX ที่ใช้ได้สำหรับสีเขียวคือ `#77fc03` ดังนั้นหากต้องการแสดงข้อความด้วยสีนี้ ให้ทำดังนี้:
`%#77fc03%this text is green!%#%`

สิ่งนี้จะแสดง `this text is green!` เป็น `#77fc03` (สีเขียว)

ตรวจสอบให้แน่ใจว่าสี HEX เริ่มต้นด้วย `#`!

รองรับชื่อสีในรูปแบบคล้าย HTML ที่ใช้ร่วมกับรหัสการจัดรูปแบบสีนี้ได้:

```
%#red%This text is red!%#%
```

ชื่อที่รองรับ: `black`, `silver`, `gray`, `grey`, `white`, `maroon`, `red`, `purple`, `fuchsia`, `magenta`, `green`, `lime`, `olive`, `yellow`, `navy`, `blue`, `teal`, `aqua`, `cyan` และ `transparent`

## การจัดแนวข้อความ

คุณสามารถจัดแนวบรรทัดข้อความได้โดยเริ่มบรรทัดด้วยรหัสการจัดแนวที่ต้องการ จากนั้นไม่มีข้อความอื่น แล้วตามด้วยบรรทัดข้อความที่คุณต้องการแสดงด้วยการจัดแนวนั้น ๆ และใส่รหัสการจัดแนวอีกครั้งในบรรทัดว่างด้านหลัง

เนื้อหาข้อความทั้งหมดจะ **จัดชิดซ้ายเป็นค่าเริ่มต้น** ดังนั้นจึงมีรหัสการจัดรูปแบบสำหรับ **จัดกึ่งกลาง** และ **จัดชิดขวา** เท่านั้น

### จัดกึ่งกลาง

หากต้องการจัดข้อความให้อยู่กึ่งกลาง ให้ใช้รหัสการจัดรูปแบบ `^^^`

ตัวอย่าง:
```
This text is not centered.

^^^
This text is centered.
This text is also centered.
^^^

This text is not centered anymore.
```

### จัดชิดขวา

หากต้องการแสดงบรรทัดข้อความแบบจัดชิดขวา ให้ใช้รหัสการจัดรูปแบบ `|||`

ตัวอย่าง:
```
This text is not right-aligned.

|||
This text is right-aligned.
This text is also right-aligned.
|||

This text is not right-aligned anymore.
```

## หัวเรื่อง

หากต้องการแสดง **บรรทัดข้อความ** เป็นหัวเรื่อง (ใหญ่ขึ้นและมีเส้นขีดใต้) ให้ใส่ `# ` (ใหญ่มาก), `## ` (ใหญ่) หรือ `### ` (เล็ก) ไว้ก่อนบรรทัดข้อความ

ตัวอย่าง:
`## Big Headline`

## ตัวหนา

ใส่ `**` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็น **ตัวหนา**

ตัวอย่าง:
`**bold text content**`

## ตัวเอียง

ใส่ `_` หรือ `*` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็น *ตัวเอียง*

ตัวอย่าง:
`*italic text content*`

## ขีดฆ่า

ใส่ `~` ไว้ก่อนและหลังข้อความเพื่อทำให้เป็นข้อความแบบ ~~ขีดฆ่า~~

ตัวอย่าง:
`~strikethrough text content~`

## ไฮเปอร์ลิงก์

คุณสามารถเพิ่มไฮเปอร์ลิงก์ลงในเนื้อหาข้อความ ซึ่งจะเปิดเว็บไซต์เมื่อคลิก

ข้อความที่ต้องการให้แสดงเป็น [ไฮเปอร์ลิงก์](https://google.com) ต้องครอบด้วย `[ ]` แล้วตามด้วยลิงก์จริงที่ครอบด้วย `( )`

ดังนั้นถ้าคุณต้องการให้ `example text content` คลิกได้และเปิด `https://example-website.net` ให้ทำดังนี้:
`[example text content](https://example-website.net)`

## เหตุการณ์เมื่อคลิกและเมื่อโฮเวอร์

เหตุการณ์คลิกและโฮเวอร์ของ Markdown ใช้ได้กับ [Text elements](./elements#text) และข้อความ Markdown อื่น ๆ ใช้ [**On Markdown Text Clicked**](./listeners#on-markdown-text-clicked-text_clicked) และ [**On Markdown Text Hovered**](./listeners#on-markdown-text-hovered-text_hovered) เพื่อรับมือกับเหตุการณ์เหล่านี้

เหตุการณ์คลิกใช้คำนำหน้า `click:`

```
[some clickable text](click:unique_text_click_event_id)
```

เหตุการณ์โฮเวอร์ใช้คำนำหน้า `hover:`

```
[some hoverable text](hover:unique_text_hover_event_id)
```

ทั้งสอง listener จะเปิดเผยรหัสเหตุการณ์เป็น `$$text_event_id`

## รูปภาพ

Markdown รองรับการแสดงรูปภาพในเนื้อหาข้อความ

FancyMenu รองรับทรัพยากรของ Minecraft, ทรัพยากรในเครื่อง และทรัพยากรจากเว็บใน Markdown

หากต้องการเพิ่มรูปภาพ ให้เริ่มบรรทัดข้อความด้วย `![](` จากนั้นตามด้วย [URL, ตำแหน่งทรัพยากร หรือพาธไปยังทรัพยากร](./resources) แล้วปิดด้วย `)`

ดังนั้น หากต้องการแสดงทรัพยากรจากเว็บ `https://example-website.net/image.png` ให้ทำดังนี้:
`![](https://example-website.net/image.png)`

รูปภาพยังสามารถเป็น **ไฮเปอร์ลิงก์** ได้ โดยครอบบรรทัดรูปภาพทั้งหมดด้วย **ไฮเปอร์ลิงก์** แบบนี้:
`[![](https://example-website.net/image.png)](https://example-website.net)`

> [!WARNING]
> ทรัพยากรในเครื่องต้องอยู่ใน `<game-directory>/config/fancymenu/assets/`!

## คำอ้างอิง

หากต้องการจัดรูปแบบข้อความเป็นคำอ้างอิง ให้เริ่มบรรทัดข้อความด้วย `> `
การจัดรูปแบบนี้จะใช้กับบรรทัดถัด ๆ ไปทั้งหมดจนกว่าจะพบบรรทัดที่ **ว่าง**

ตัวอย่าง:
```
This will not look like a quote.

> This will look like a quote.
This will also look like a quote.

This will not look like a quote anymore.
```

## รายการแบบหัวข้อย่อย

หากต้องการแสดงข้อความเป็นรายการแบบหัวข้อย่อย เช่นนี้:
- Entry 1
- Entry 2
  - Sub-Entry

คุณเพียงแค่ต้องเริ่มบรรทัดด้วย `- `

ตัวอย่าง:
```
- Entry 1
- Entry 2
  - Sub-Entry
```

## เส้นแบ่ง

หากต้องการเพิ่มเส้นแบ่งในข้อความที่มีความกว้างเท่ากับหนึ่งบรรทัดข้อความเต็ม ๆ เพียงแค่เริ่มบรรทัดด้วย `---` และไม่ต้องใส่อะไรต่อ

จากนั้นจะมีลักษณะประมาณนี้:

---

## บล็อกโค้ด

บล็อกโค้ดช่วยให้คุณแสดงข้อความเป็น `plain text` โดยไม่ให้ Markdown พยายามจัดรูปแบบ หรือใช้เพื่อแสดงข้อความในสไตล์คล้ายโค้ดโดยไม่ตัดบรรทัดอัตโนมัติ

บล็อกโค้ดบรรทัดเดียว (อยู่ระหว่างข้อความอื่น) เริ่มและจบด้วย \` ซึ่งจริง ๆ แล้วค่อนข้างยากที่จะใส่แสดงในข้อความ Markdown..

บรรทัดข้อความที่มีบล็อกโค้ดบรรทัดเดียวจะมีลักษณะดังนี้:

![single_line_code_block](https://github.com/Keksuccino/FancyMenu/assets/35544624/e78fd38b-7b1a-4f00-9b11-797d964b3b43)

บล็อกโค้ดหลายบรรทัดจะครอบหลายบรรทัดไว้ในบล็อกโค้ดขนาดใหญ่หนึ่งบล็อก โดยเริ่มด้วยบรรทัดที่มีเพียง \`\`\` จากนั้นเป็นเนื้อหาข้อความ แล้วตามด้วย \`\`\` อีกครั้ง:

![multi_line_code_block](https://github.com/Keksuccino/FancyMenu/assets/35544624/bf8c77eb-a97e-48cd-9270-8302c2995856) 

## ข้อความธรรมดา

รหัสการจัดรูปแบบข้อความธรรมดาจะข้ามรหัสการจัดรูปแบบอื่น ๆ ทั้งหมดที่อยู่ภายใน

มันทำงานคล้ายกับบล็อกโค้ด แต่จะไม่แสดงผลเป็นบล็อกโค้ด แทนที่จะเป็นเช่นนั้น มันจะแสดงเหมือนข้อความปกติ แต่ไม่มีการจัดรูปแบบใด ๆ

หากต้องการครอบบางส่วนของข้อความภายในบรรทัดด้วยรหัสการจัดรูปแบบข้อความธรรมดา ให้ใส่ `;;` ไว้ก่อนและหลังส่วนของข้อความที่คุณต้องการแสดงเป็นข้อความธรรมดา เช่นนี้:

```
This is a line of text with ;;**this part**;; showing as unformatted text with visible ** (bold) formatting code and _this part_ as normal formatted italic text.
```

ข้อความธรรมดายังใช้เป็นรหัสครอบหลายบรรทัดได้ด้วย หากต้องการครอบทั้งบรรทัด ให้ใส่ `;;;` ไว้ก่อนและหลังบรรทัดที่คุณต้องการแสดงเป็นข้อความธรรมดา เช่นนี้:

```
;;;
This line will show **unformatted** with visible ** (bold) formatting codes.
This line will also show _unformatted_ with visible _ (italic) formatting codes.
;;;

This line will look **normal** again with the "normal" formatted as bold text.
```

# การจัดรูปแบบข้อความของ Minecraft

รหัสการจัดรูปแบบของ Minecraft ใช้ได้กับฟิลด์ข้อความที่รองรับการจัดรูปแบบทั่วทั้ง FancyMenu ใช้ `&` แทนคำนำหน้า `§` ของ Minecraft; ตัวอย่างเช่น `&cWarning` จะแสดงข้อความเป็นสีแดง

ดู [เอกสารอ้างอิงรหัสการจัดรูปแบบใน Minecraft Wiki](https://minecraft.wiki/w/Formatting_codes) สำหรับสีและสไตล์ที่ใช้งานได้

> [!CAUTION]
> รหัสการจัดรูปแบบของ Minecraft ไม่น่าเชื่อถือใน **Text elements** เนื่องจากองค์ประกอบเหล่านั้นจะแปลง Markdown ก่อน ให้ใช้การจัดรูปแบบ Markdown ที่อธิบายไว้ข้างต้นแทน

# คอมโพเนนต์ข้อความของ Minecraft (ระบบคอมโพเนนต์ดิบ)

ระบบคอมโพเนนต์ข้อความของ Minecraft มีความทรงพลังมากสำหรับเนื้อหาข้อความ **บรรทัดเดียว** เช่น **ป้ายกำกับปุ่ม**

ใน Vanilla Minecraft คุณสามารถใช้ได้ในคำสั่ง `/tellraw` และ `/title` (และอาจมีที่อื่นด้วย)
เป็นข้อความที่จัดรูปแบบแล้วและถูกทำให้เป็น JSON ดังนั้นคุณจึงสามารถเพิ่มแอตทริบิวต์การจัดรูปแบบลงในเนื้อหาข้อความได้

หากต้องการเรียนรู้เพิ่มเติมเกี่ยวกับคอมโพเนนต์ข้อความโดยละเอียด โปรดดู [หน้า Minecraft wiki นี้](https://minecraft.wiki/w/Raw_JSON_text_format)
หากต้องการเรียนรู้เพิ่มเติมเกี่ยวกับฟอนต์ใน Minecraft โปรดดู [หน้า Minecraft wiki นี้](https://minecraft.wiki/w/Resource_pack#Fonts)

หากต้องการให้ FancyMenu ตรวจจับป้ายกำกับปุ่มเป็น **คอมโพเนนต์ข้อความ** ให้ใส่เฉพาะข้อความคอมโพเนนต์ที่ serialize แล้วเป็นป้ายกำกับเท่านั้น แบบนี้:
`{"text":"Button Label Text","font":"uniform"}`

ตัวอย่างข้างต้นจะแสดงป้ายกำกับปุ่ม `Button Label Text` ด้วยฟอนต์ `uniform`

> [!NOTE]
> คุณสามารถใช้ placeholder ของ FancyMenu ในค่า `text` ของคอมโพเนนต์ได้
