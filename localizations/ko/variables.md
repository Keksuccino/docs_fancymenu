---
title: 변수
description: 변수를 만들고 사용하는 방법.
---

# FancyMenu의 변수

변수는 FancyMenu에서 정보를 저장하고 메뉴 커스터마이징 전반에 걸쳐 다시 사용할 수 있게 해 주는 강력한 기능입니다. 변수는 서로 다른 종류의 데이터를 넣을 수 있는 컨테이너처럼 작동하며, 각 컨테이너에 이름을 붙인 뒤 나중에 변수 이름으로 해당 데이터에 접근할 수 있습니다. 변수는 사용자가 정의한 조건에 따라 바뀌는 동적인 메뉴를 만드는 데 무한한 가능성을 열어 줍니다.

## 변수 만들기

FancyMenu에서 변수를 만들려면:

1. 현재 레이아웃 편집기(Layout Editor) 안에 있지 않은지 확인합니다.
2. 화면 상단의 메뉴 바를 클릭합니다.
3. **Customization -> Variables -> Manage Variables**로 이동합니다.
4. 표시되는 "Manage Variables" 화면에서 **Add Variable** 버튼을 클릭합니다.
5. 새 변수의 이름을 입력하고 **OK**를 클릭합니다.

이렇게 끝입니다! 이제 변수를 사용할 준비가 되었습니다. 해당 변수는 "Manage Variables" 화면 목록에서 확인할 수 있습니다.

FancyMenu 3.9.0에서는 Manage Variables 창이 새롭게 개편되었습니다. 중요한 작업은 마우스 오른쪽 버튼 컨텍스트 메뉴를 통해 사용할 수 있으며, 목록은 키보드 탐색을 지원하고, 변수는 복사/붙여넣기할 수 있으며, 변경 사항은 실행 취소/다시 실행할 수 있고, 입력을 시작하면 검색이 시작되며, **DEL** 키로 선택한 변수를 삭제할 수 있고, **CTRL + S**로 창을 확인합니다.

## 변수 값 설정하기

빈 변수는 그 자체로는 별로 유용하지 않습니다. 변수를 제대로 활용하려면 그 안에 데이터를 넣어야 합니다. FancyMenu에서는 이를 "변수 값 설정"이라고 합니다.

변수 값을 설정하는 방법은 크게 두 가지가 있습니다:

1. "Manage Variables" 화면에서 목록에서 변수를 찾은 뒤 클릭하고, **Set Value**를 클릭합니다. 저장할 데이터를 입력합니다.

2. 메뉴를 커스터마이징하는 동안 Button, Slider 또는 Ticker 요소에 **Set Variable** 동작을 사용합니다. 이 동작에서는 변수 이름과 그 안에 저장할 값을 지정합니다. 예를 들어 누군가 이 동작이 설정된 버튼을 클릭하면, 변수는 새 값으로 업데이트됩니다.

예를 들어, 버튼이 눌린 횟수를 세기 위해 `clicks`라는 변수를 만든다고 해 봅시다. 버튼에 **Set Variable** 동작을 추가하고, 동작의 값에 플레이스홀더를 사용해 클릭 횟수를 매번 1씩 늘리면 됩니다. 예시는 다음과 같습니다:

```
clicks:{"placeholder":"calc","values":{"expression":"{"placeholder":"getvariable","values":{"name":"clicks"}} + 1"}}
```

작동 방식은 다음과 같습니다:
1. **Get Stored Variable** 플레이스홀더가 `clicks` 변수의 현재 값을 가져옵니다.
2. **Calculator** 플레이스홀더가 그 값에 1을 더합니다.
3. 그 결과는 **Set Variable** 동작을 사용해 다시 `clicks` 변수에 저장됩니다.

따라서 버튼을 클릭할 때마다 `clicks` 변수는 1씩 증가하여, 총 클릭 횟수를 효과적으로 세게 됩니다.

## 변수 사용하기

이제 변수가 데이터를 담고 있으므로, 메뉴 커스터마이징의 다양한 부분에서 그 데이터를 사용할 수 있습니다:

* **로딩 요구 사항(Loading Requirements)**: 변수 값을 로딩 요구 사항에서 확인하여 특정 메뉴 요소가 언제 나타날지 제어할 수 있습니다. 예를 들어, **Is Number** 요구 사항과 **Get Stored Variable** 플레이스홀더를 조합해 `clicks` 변수가 5보다 클 때만 요소를 표시하도록 만들 수 있습니다.

* **플레이스홀더**: **Get Stored Variable** 플레이스홀더를 사용하면 변수 값을 텍스트에 삽입할 수 있습니다. 텍스트 요소가 있다면 `{"placeholder":"getvariable","values":{"name":"clicks"}}`를 사용해 "clicks" 변수의 현재 값을 표시할 수 있습니다.

* **중첩 플레이스홀더**: 다른 플레이스홀더 안에서도 변수를 사용할 수 있습니다! 위의 클릭 횟수 예시는 **Get Stored Variable** 플레이스홀더를 **Calculator** 플레이스홀더 안에서 사용해 이를 보여 주었습니다.

* **동작(Actions)**: 변수는 동작에서 사용되어 변수 값에 따라 달라지는 동적인 행동을 만들 수 있습니다. 몇 가지 예시는 다음과 같습니다:
    - **IF** 문을 동작 스크립트에 사용해 **Is Number** 로딩 요구 사항과 **Get Stored Variable** 동작을 조합하여 변수 값을 확인하고, 결과에 따라 서로 다른 동작을 수행할 수 있습니다. 예를 들어 "You've clicked me X times!"라고 표시하는 버튼을 만들고, 클릭 횟수가 10보다 크면 특별한 메시지를 보여 주도록 IF 블록을 사용할 수 있습니다.
    - **Get Stored Variable** 플레이스홀더를 **Copy to Clipboard** 동작과 결합해 사용자가 변수 값을 클립보드에 복사할 수 있게 할 수 있습니다.
    - **Open GUI** 동작에서 변수를 사용해 사용자의 진행 상황이나 선호도에 따라 다른 화면을 불러올 수 있으며, 이 정보는 변수로 추적합니다.

## 변수 예시

다음은 직접 변수 사용법을 구상해 볼 수 있는 몇 가지 예시입니다:

1. **최고 점수**: `highscore` 변수를 만들고, 현재 점수가 기존 값보다 높으면 플레이어의 현재 점수로 설정하는 버튼을 만듭니다. **Get Stored Variable** 플레이스홀더를 사용해 메뉴에 최고 점수를 표시합니다.

2. **난이도 선택기**: `easy`, `medium`, `hard`처럼 서로 다른 게임 난이도에 대한 변수를 만듭니다. 버튼으로 난이도 변수를 설정하고, 선택된 난이도에 따라 요소를 표시하거나 숨깁니다.

3. **튜토리얼 진행도**: `tutorial_step`처럼 튜토리얼 진행 상황을 추적하는 변수를 추가합니다. 각 단계를 완료할 때마다 변수를 증가시키고, 로딩 요구 사항을 사용해 메뉴의 더 많은 부분을 점진적으로 표시합니다.

변수는 FancyMenu의 다른 기능들과 결합하면 각 플레이어의 행동과 선호도에 맞춘 메뉴를 만들 수 있는 놀라운 유연성을 제공합니다. 다양한 변수 구성을 실험해 보며 메뉴 커스터마이징의 잠재력을 최대한 끌어내 보세요!
