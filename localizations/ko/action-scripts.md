---
title: 액션 스크립트
description: '버튼, 슬라이더, 티커 등에서 액션 스크립트를 사용하는 방법입니다.'
---

# 액션 스크립트

FancyMenu를 사용하면 요소에 **액션**을 할당하여 메뉴에 상호작용을 추가할 수 있습니다. 이러한 액션은 버튼을 클릭할 때, 티커가 동작할 때, 슬라이더를 사용할 때, 또는 화면이 열리거나 닫힐 때 실행됩니다. 또한 **if**, **else-if**, **else**, **while** 같은 간단한 제어문을 사용해 고급 액션 스크립트를 만들어, 어떤 액션이 언제 실행될지 제어할 수도 있습니다.

<img src="https://github.com/Keksuccino/FancyMenu/blob/master/assets/docs/action_script_editor.png?raw=true" alt="Action script editor" style="max-width:800px;width:100%;height:auto;">

# 액션이란?

**액션**은 트리거될 때 FancyMenu가 실행하는 작업 또는 기능입니다. 예를 들어 액션은 새 화면을 열거나, 채팅 메시지를 보내거나, 오디오 요소의 볼륨을 조절할 수 있습니다. FancyMenu의 편집기에서는 액션에 필요에 따라 값(value)을 설정하여 URL이나 서버 주소 같은 추가 정보를 제공할 수 있습니다.

# 문(statement)이란?

더 복잡한 동작을 만들기 위해 FancyMenu는 액션 스크립트에서 기본적인 제어문을 지원합니다. 여기에는 다음이 포함됩니다:

- **If 문:** 지정된 [조건](/en/conditions)이 충족될 때만 액션 블록을 실행합니다.
- **Else-If 문:** 앞선 *if* (또는 이전 *else-if*)가 충족되지 않았을 때 다른 [조건](/en/conditions)을 확인합니다.
- **Else 문:** 앞선 어떤 [조건](/en/conditions)도 충족되지 않았을 때 실행됩니다.
- **While 문:** [조건](/en/conditions)이 참인 동안 액션 블록을 계속 반복 실행합니다(무한 루프 방지를 위한 내장 타임아웃 포함).
- **Delay 블록:** 포함된 액션을 실행하기 전에 지정된 시간만큼 기다립니다. 지연이 진행되는 동안 나머지 스크립트는 계속 실행됩니다.
- **Execute Later 블록:** 포함된 액션을 밀리초 지연 후 메인 스레드에서 실행하도록 큐에 넣습니다.
- **Comment:** 정리를 위해 스크립트 안에 메모를 추가합니다. Comment는 어떤 액션도 실행하지 않습니다.

이러한 문을 액션과 조합하면, 예를 들어 플레이어의 체력이 낮을 때 경고 메시지를 보내거나, 조건이 바뀔 때까지 업데이트를 반복하는 식의 동적이고 조건적인 동작을 만들 수 있습니다.

# 액션 스크립트를 어디에 사용할 수 있나요?

액션 스크립트는 매우 유연하며 레이아웃 전반에서 사용할 수 있습니다. 예를 들어 다음에 할당할 수 있습니다:

- **버튼:** 버튼을 클릭했을 때 액션을 실행합니다.
- **티커:** 레이아웃 안의 화면 정보를 갱신하기 위해 액션 스크립트를 계속 실행합니다.
- **슬라이더:** 슬라이더 값이 바뀔 때마다 액션 스크립트를 트리거합니다.
- **화면 이벤트:** 화면이 열리거나 닫힐 때 스크립트를 실행합니다(예: 메뉴가 나타날 때 소리를 재생).
- **리스너:** 특정 이벤트를 감지하는 리스너가 실행되면 해당 액션 스크립트를 실행합니다.
- **스케줄러:** 화면이 열려 있지 않아도 정해진 시간에 따라 액션을 실행합니다.

# 액션에서 플레이스홀더 사용하기

액션 값은 **플레이스홀더**를 통해 동적 콘텐츠를 지원합니다. 대부분의 경우 이 플레이스홀더는 JSON과 비슷한 문법을 사용하며, 액션이 실행될 때 실제 데이터로 치환됩니다.

## JSON 스타일 플레이스홀더

이것들은 레이아웃 전반의 여러 위치에서 사용할 수 있는 일반적인 [플레이스홀더](/en/placeholders)입니다.

형식은 다음과 같습니다:

```json
{"placeholder": "placeholder_id", "values": {"key1": "value1", "key2": "value2"}}
```

이들은 플레이어 이름, 화면 크기, 또는 **Calculator** 플레이스홀더로 계산한 값 같은 게임 데이터를 가져올 수 있습니다. 더 고급 사용을 위해 플레이스홀더를 중첩해서 사용할 수도 있습니다.

## `$$` 플레이스홀더(변수)

`$$` 플레이스홀더는 특별합니다. FancyMenu의 일부 기능은 중첩 액션, 요구사항, 일반 플레이스홀더를 위해 이러한 특수 플레이스홀더를 제공하며, 이를 내부에서 사용해 자신이 속한 환경(요소, 리스너 등)에 대한 추가 정보를 얻을 수 있습니다.

예를 들어 슬라이더 안에서 액션을 사용할 때, 액션에서 `$$value`를 사용하면 슬라이더의 현재 값으로 치환됩니다.

리스너에서 액션을 사용할 때는, 각 리스너가 눌린 마우스 버튼, 입력된 구조 등 리스너에 대한 더 많은 정보를 얻기 위해 고유한 변수/플레이스홀더 집합을 제공합니다.

# 액션을 설정하고 편집하는 방법

요소에 대한 액션(및 문 블록)을 추가, 수정 또는 제거하려면, **해당 요소를 우클릭**한 다음(버튼, 슬라이더, 티커 또는 다른 상호작용 요소) **Manage Action Script**를 선택하면 됩니다. 그러면 Manage Actions 화면이 열리며, 여기서 다음을 할 수 있습니다:

- **새 액션 또는 문 추가:** 스크립트를 만들기 위해 새로운 액션 항목이나 제어문(if, else-if, else, while)을 삽입합니다.
- **기존 액션 또는 문 편집:** 액션 값을 수정하거나 제어 로직을 변경합니다.
- **액션 또는 문 제거:** 원하지 않는 액션을 스크립트에서 삭제합니다.

[리스너](/listeners)에는 리스너를 관리하고 생성할 수 있는 별도의 메뉴가 있으며, 액션 스크립트에 접근하는 기능도 포함되어 있어 예를 들어 버튼이나 슬라이더의 액션 스크립트를 편집할 때와 같은 경험을 할 수 있습니다.

> Action Script Editor 화면에서는 큰 짙은 회색 영역을 우클릭하기만 하면 액션, 문, 기타 항목을 추가하는 컨텍스트 메뉴를 열 수 있습니다.
{.is-info}


# 액션 스크립트 편집기 단축키 및 기타

액션 스크립트 편집기에는 스크립트 편집을 아주 쉽게 만들어 주는 유용한 편의 기능들이 있습니다.

## 단축키

- `DEL` : 선택한 항목을 빠르게 삭제
- `ENTER` : 선택한 항목의 인라인 편집을 시작함(또는 선택한 항목에 인라인 편집이 없으면 편집 화면을 염)
- `CTRL + C` : 선택한 액션 복사(현재는 액션에만 작동)
- `CTRL + V` : 이전에 복사한 액션 붙여넣기
- `CTRL + Z` : 한 단계 뒤로(실행 취소)
- `CTRL + Y` : 한 단계 앞으로(다시 실행)
- `ARROW UP` : 현재 선택한 항목에서 한 칸 위로 이동
- `ARROW DOWN` : 현재 선택한 항목에서 한 칸 아래로 이동
- `SHIFT + ARROW UP` : 선택한 항목을 한 칸 위로 이동
- `SHIFT + ARROW DOWN` : 선택한 항목을 한 칸 아래로 이동
- `A` : 새 액션을 추가하기 위해 Action Chooser 화면을 빠르게 열기
- `CTRL + S` : 편집기 창에서 완료/저장

## 추가 편의 기능

- 액션의 값을 더블클릭하면 전체 값 편집 화면으로 들어가지 않고도 값을 편집할 수 있습니다.
- IF 문 체인(뒤에 ELSE/ELSE-IF 문이 붙은 것), WHILE 루프, 폴더는 접을 수 있습니다(시각적인 기능일 뿐, 스크립트 로직에는 영향을 주지 않음).
- 편집기는 항상 선택한 항목 아래에 새 액션을 추가합니다(또는 선택한 체인/루프/폴더 내부에 중첩하여 추가).
- 짙은 회색 스크립트 영역 배경을 우클릭하면 액션, 문 및 기타 중요한 항목을 추가하는 옵션이 있는 컨텍스트 메뉴가 열립니다.

# 액션 상세

이 목록에는 FancyMenu에서 사용할 수 있는 대부분, 혹은 전부의 액션이 포함되어 있습니다. 모드 업데이트로 인해 목록이 가끔 약간 오래되었을 수 있습니다.

## 다음 트랙 (`audio_next_track`)
- **설명:** 오디오 요소에서 다음 트랙으로 이동합니다
- **필수 값:** 예 - `audio_element_identifier`(제어할 오디오 요소의 ID)

## 이전 트랙 (`audio_previous_track`)
- **설명:** 오디오 요소에서 이전 트랙으로 이동합니다
- **필수 값:** 예 - `audio_element_identifier`(제어할 오디오 요소의 ID)

## 트랙 볼륨 설정 (`set_audio_element_volume`)
- **설명:** 오디오 요소의 볼륨을 설정합니다(0.0 ~ 1.0)
- **필수 값:** 예 - `element_identifier:volume`

## 재생/일시정지 토글 (`audio_toggle_play`)
- **설명:** 오디오 요소의 현재 트랙 재생/일시정지를 전환합니다
- **필수 값:** 예 - `audio_element_identifier`

## 오디오 재생 (`play_audio`)
- **설명:** 오디오 리소스를 한 번 재생합니다. 이 액션은 시작한 오디오를 추적하므로 나중에 `stop_all_action_audios`로 중지할 수 있습니다.
- **필수 값:** 예 - `audioSource`, `soundChannel`, `baseVolume`가 포함된 JSON 설정
- **예시 값:** `{"audioSource":"[source:local]/config/fancymenu/assets/example.ogg","soundChannel":"master","baseVolume":1.0}`

## 모든 액션 오디오 중지 (`stop_all_action_audios`)
- **설명:** **오디오 재생** 액션으로 시작된 모든 오디오 트랙을 중지합니다. 오디오 요소, 메뉴 열기/닫기 소리, 버튼 소리 또는 다른 오디오 시스템은 중지하지 않습니다.
- **필수 값:** 아니요

## 비디오 요소 볼륨 설정 (`set_video_element_volume`)
- **설명:** 비디오 요소의 볼륨을 설정합니다(0.0 ~ 1.0)
- **필수 값:** 예 - `video_element_identifier:volume`

## 비디오 요소 재생 시간 설정 (`set_video_element_play_time`)
- **설명:** 비디오 요소를 밀리초 타임스탬프로 이동합니다
- **필수 값:** 예 - `video_element_identifier:timestamp_ms`

## 비디오 요소 일시정지 상태 토글 (`toggle_video_element_pause_state`)
- **설명:** 비디오 요소의 일시정지 상태를 전환합니다
- **필수 값:** 예 - `video_element_identifier`

## 비디오 배경 볼륨 설정 (`set_video_menu_background_volume`)
- **설명:** 비디오 메뉴 배경의 볼륨을 설정합니다(0.0 ~ 1.0)
- **필수 값:** 예 - `background_identifier:volume`

> 배경의 식별자를 얻으려면 편집기 배경을 우클릭한 뒤 'Copy Background Identifier'를 클릭하세요.
{.is-info}

## 비디오 배경 재생 시간 설정 (`set_video_menu_background_play_time`)
- **설명:** 비디오 메뉴 배경을 밀리초 타임스탬프로 이동합니다
- **필수 값:** 예 - `background_identifier:timestamp_ms`

> 배경의 식별자를 얻으려면 편집기 배경을 우클릭한 뒤 'Copy Background Identifier'를 클릭하세요.
{.is-info}

## 비디오 배경 일시정지 상태 토글 (`toggle_video_menu_background_pause_state`)
- **설명:** 비디오 메뉴 배경의 일시정지 상태를 전환합니다
- **필수 값:** 예 - `background_identifier`

> 배경의 식별자를 얻으려면 편집기 배경을 우클릭한 뒤 'Copy Background Identifier'를 클릭하세요.
{.is-info}

## 레이아웃 토글 (`toggle_layout`)
- **설명:** 이름으로 레이아웃을 켜거나 끕니다(Enable/Disable)
- **필수 값:** 예 - `layout_name`

## 레이아웃 활성화 (`enable_layout`)
- **설명:** 이름으로 레이아웃을 활성화합니다
- **필수 값:** 예 - `layout_name`

## 레이아웃 비활성화 (`disable_layout`)
- **설명:** 이름으로 레이아웃을 비활성화합니다
- **필수 값:** 예 - `layout_name`

## 화면 또는 사용자 지정 GUI 열기 (`opengui`)
- **설명:** 식별자로 화면을 엽니다(바닐라, 모드, 또는 사용자 지정 GUI)
- **필수 값:** 예 - `screen_identifier`

> 이 액션은 **모든 화면에서 작동하지는 않습니다**, 특히 모드 화면에서 그렇습니다. 액션이 화면 열기에 실패하면 오류가 표시됩니다. 이 경우 할 수 있는 일은 많지 않습니다. 아마도 FancyMenu가 자동으로 열기에는 너무 복잡한 화면일 가능성이 큽니다.
> 
> 또한 모드 화면에 대한 호환성은 더 이상 FancyMenu 측에서 수동으로 추가되지 않습니다. 존재하는 모든 모드에 대해 호환성을 추가하려면 너무 많은 시간이 걸리기 때문입니다. 죄송합니다. 대부분의 경우 이런 상황에서 다른 모드의 개발자에게 문의하는 것도 권장되지 않습니다. FancyMenu가 화면을 열 수 없다면, 지원을 추가하는 쉬운 방법이 없기 때문입니다. 이 경우 권장되는 우회 방법은 **"Mimic Vanilla/Mod Button"** 액션을 사용해 해당 화면을 여는 특정 버튼을 흉내 내는 것입니다. 버튼이 없다면, 안타깝지만 방법이 없습니다.
{.is-info}

## 화면 닫기 (`closegui`)
- **설명:** 현재 활성화된 화면을 닫습니다
- **필수 값:** 아니요

## 화면 업데이트 (`update_screen`)
- **설명:** 현재 화면을 다시 초기화합니다
- **필수 값:** 아니요

## 이전 화면으로 돌아가기 (`back_to_last_screen`)
- **설명:** 이전 화면(현재 화면의 바로 전 화면)으로 돌아갑니다
- **필수 값:** 아니요

## 서버 접속 (`joinserver`)
- **설명:** 플레이어를 Minecraft 서버에 연결합니다
- **필수 값:** 예 - `server_ip:port`

## 월드 진입 (`loadworld`)
- **설명:** Minecraft 월드에 들어갑니다
- **필수 값:** 예 - `world_folder_name`

## 마지막 월드/서버에 들어가기/접속하기 (`join_last_world`)
- **설명:** 플레이어가 마지막으로 있던 월드 또는 서버에 들어가거나 접속합니다
- **필수 값:** 아니요

## 월드 또는 서버 나가기 (`disconnect_server_or_world`)
- **설명:** 월드 또는 서버를 나가고 지정된 화면을 엽니다
- **필수 값:** 예 - `screen_identifier`

## Minecraft 종료 (`quitgame`)
- **설명:** Minecraft를 완전히 종료합니다
- **필수 값:** 아니요

## 채팅 메시지/명령어 보내기 (`sendmessage`)
- **설명:** 채팅 메시지를 보내거나 채팅 명령어를 실행합니다
- **필수 값:** 예 - `message_text` 또는 `/command_text`

## 통합 서버로 명령어 실행 (`execute_command_as_integrated_server`)
- **설명:** 권한과 치트 설정을 무시하고 싱글플레이에서 통합 서버로 명령어를 강제로 실행합니다.
- **필수 값:** 예 - 명령어 텍스트, 예: `/give @p minecraft:diamond 1`

> 이 액션은 싱글플레이에서만 작동하며, 월드가 LAN에 공개되지 않은 상태여야 합니다. 멀티플레이 서버에서는 의도적으로 아무 작업도 하지 않습니다.
{.is-warning}

## 채팅에 붙여넣기 (`paste_to_chat`)
- **설명:** 채팅 입력창에 텍스트를 붙여넣습니다(추가 또는 교체)
- **필수 값:** 예 - `true:Text` 또는 `false:Text`

## 채팅에 표시 [클라이언트 전용] (`display_in_chat_client_side`)
- **설명:** 텍스트를 로컬 채팅에 직접 출력합니다(서버 없음)
- **필수 값:** 예 - `text_or_json`

## FM 데이터 서버로 전송 (`send_fm_data_to_server`)
- **설명:** FM Data 패킷 채널을 통해 현재 FancyMenu 서버로 사용자 지정 텍스트 데이터를 전송합니다.
- **필수 값:** 예 - `data_identifier||data`

## 원격 서버에 연결 (`connect_to_remote_server`)
- **설명:** 클라이언트가 시작하는 외부 원격 서버와의 WebSocket 연결을 열거나 재사용합니다.
- **필수 값:** 예 - 원격 서버 URL, 예: `wss://example.com/ws`

## 원격 서버로 데이터 전송 (`send_data_to_remote_server`)
- **설명:** 원격 서버 연결을 열거나 재사용한 뒤 텍스트 데이터를 전송합니다.
- **필수 값:** 예 - `remote_server_url||data`

## 원격 서버 연결 닫기 (`close_remote_server_connection`)
- **설명:** 요청 ID로 특정 원격 서버 연결을 닫습니다.
- **필수 값:** 예 - 요청 ID, 보통 `$$request_id` 같은 Remote Server 리스너 변수에서 가져옴

## 모든 원격 서버 연결 닫기 (`close_all_remote_server_connections`)
- **설명:** FancyMenu가 연 모든 활성 원격 서버 연결을 닫습니다.
- **필수 값:** 아니요

## 브라우저에서 URL 열기 (`openlink`)
- **설명:** 기본 브라우저에서 링크를 엽니다
- **필수 값:** 예 - `https://example.com`

## 클립보드에 텍스트 복사 (`copytoclipboard`)
- **설명:** 텍스트를 클립보드에 복사합니다
- **필수 값:** 예 - `text_to_copy`

## 게임 로그에 출력 (`print_to_log`)
- **설명:** 게임 로그에 한 줄을 기록합니다
- **필수 값:** 예 - `text_to_log`

## 변수 값 설정(FM 변수) (`set_variable`)
- **설명:** 텍스트 내용을 FancyMenu 변수에 저장합니다
- **필수 값:** 예 - `variable_name:variable_value`

## 모든 변수 지우기(FM 변수) (`clear_variables`)
- **설명:** FancyMenu에 저장된 모든 변수를 지웁니다
- **필수 값:** 아니요

## HTTP 요청 보내기 (`send_http_request`)
- **설명:** HTTP 요청을 보냅니다. 응답을 변수에 저장할 수도 있습니다
- **필수 값:** 예 - HTTP 요청 설정

> 이 액션을 사용하면 REST API, 웹훅 또는 모든 HTTP 엔드포인트로 데이터를 보낼 수 있습니다.
> 다양한 인증 방식, 사용자 지정 헤더, 여러 요청 유형을 지원합니다.
> 
> 또한 이 액션은 요청의 응답을 나중에 사용할 수 있도록 FancyMenu 변수에 저장할 수도 있습니다!
{.is-info}

## 리소스 팩 관리 (`manage_resource_pack`)
- **설명:** 표시 이름으로 리소스 팩을 활성화/비활성화/토글합니다(선택적 다시 로드)
- **필수 값:** 예 - `pack_name|||MODE|||reload_bool`

## 리소스 팩 다시 로드 (`reload_resource_packs`)
- **설명:** 리소스 팩을 다시 로드합니다(5초 쿨다운)
- **필수 값:** 아니요

## FancyMenu 다시 로드 (`reloadmenu`)
- **설명:** 파노라마, 슬라이드쇼 및 모든 리소스를 포함해 FancyMenu를 다시 로드합니다(무거움)
- **필수 값:** 아니요

> 이 액션은 **성능에 큰 영향**을 주며, 티커에서 사용하면 렉이 발생할 수 있습니다. 버튼 외의 다른 곳에서 사용하는 것은 권장되지 않습니다.
{.is-warning}

## 요소 애니메이터 토글 (`toggle_element_animator`)
- **설명:** 요소 애니메이터의 재생 상태를 전환합니다
- **필수 값:** 예 - `animator_identifier`

## 요소 애니메이터 활성화 (`enable_element_animator`)
- **설명:** 요소 애니메이터를 활성화합니다
- **필수 값:** 예 - `animator_identifier`

## 요소 애니메이터 비활성화 (`disable_element_animator`)
- **설명:** 요소 애니메이터를 비활성화합니다
- **필수 값:** 예 - `animator_identifier`

## 요소 애니메이터 초기화 (`reset_element_animator`)
- **설명:** 요소 애니메이터의 타임라인/상태를 초기화합니다
- **필수 값:** 예 - `animator_identifier`

## 바닐라/모드 버튼 흉내 내기 (`mimicbutton`)
- **설명:** 바닐라 또는 모드 버튼의 클릭 동작을 흉내 냅니다
- **필수 값:** 예 - `screen_identifier:widget_locator`

## 키바인드 흉내 내기 (`mimic_keybind`)
- **설명:** Minecraft 키바인드를 실행합니다(선택적으로 유지 가능)
- **필수 값:** 예 - `keybind_id|||keep_pressed_bool|||duration_ms`

## 텍스트 입력 필드 값 설정 (`set_text_input_field_value`)
- **설명:** 요소 식별자로 사용자 지정 또는 바닐라 입력 필드의 값을 설정합니다.
- **필수 값:** 예 - `element_identifier|||new_value|||force_set_when_inactive`

## 게임 디렉터리에 파일 만들기 (`create_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트)에 빈 파일을 만듭니다. 기본 런처 프로필 디렉터리를 대상으로 할 수 있도록 `.minecraft/` 접두사를 지원합니다(현재 인스턴스 디렉터리와 다를 수 있음).
- **필수 값:** 예 - `file_path`

## 게임 디렉터리에서 파일/폴더 삭제 (`delete_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트)의 파일 또는 폴더를 삭제합니다. 기본 런처 프로필을 대상으로 하려면 `.minecraft/` 접두사를 사용할 수 있습니다(실행 중인 인스턴스와 다를 수 있음). 폴더 바로 아래의 **모든 파일**을 삭제하려면 경로 끝에 `*`를 추가하세요(하위 디렉터리는 무시되며, 폴더는 유지됨).
- **필수 값:** 예 - `target_path`

## 게임 디렉터리에서 파일/폴더 복사 (`copy_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트) 안에서 복사합니다. `.minecraft/` 접두사는 기본 런처 프로필을 대상으로 합니다(현재 인스턴스와 항상 같지는 않음). 폴더 바로 아래의 모든 파일을 복사하려면 **원본** 경로 끝에 `*`를 추가하세요(하위 디렉터리는 무시됨). 대상은 디렉터리여야 하며 `*`를 사용할 수 없습니다.
- **필수 값:** 예 - `source||destination`

## 게임 디렉터리에서 파일/폴더 이동 (`move_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트) 안에서 이동합니다. `.minecraft/` 접두사는 기본 런처 프로필을 대상으로 합니다(현재 인스턴스와 다를 수 있음). 폴더 바로 아래의 모든 파일을 이동하려면 **원본** 경로 끝에 `*`를 추가하세요(하위 디렉터리는 무시됨). 대상은 디렉터리여야 하며 `*`를 사용할 수 없습니다.
- **필수 값:** 예 - `source||destination`

## 게임 디렉터리에서 파일/폴더 이름 바꾸기 (`rename_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트) 안의 파일 또는 폴더 이름을 바꿉니다. `.minecraft/` 접두사는 기본 런처 프로필을 대상으로 합니다(현재 인스턴스와 다를 수 있음). 내용은 그대로 유지되고 이름만 변경됩니다.
- **필수 값:** 예 - `path||new_name`

## 게임 디렉터리에 파일 다운로드 (`download_file_to_game_dir`)
- **설명:** 비동기적으로 파일을 게임 디렉터리(인스턴스 루트)로 다운로드합니다. `.minecraft/` 접두사는 기본 런처 프로필을 대상으로 합니다(반드시 현재 실행 중인 인스턴스와 같지는 않음). **대상 폴더**를 지정하세요. 파일명은 헤더/URL에서 자동으로 결정됩니다.
- **필수 값:** 예 - `url||target_folder`

## 게임 디렉터리에서 ZIP 파일 압축 풀기 (`extract_zip_file_in_game_dir`)
- **설명:** ZIP 파일을 게임 디렉터리 또는 기본 `.minecraft` 디렉터리 내부의 대상 폴더로 압축 해제합니다. 완료되면 **On ZIP Extracted via Action** 리스너를 트리거합니다.
- **필수 값:** 예 - `source_zip_path||target_folder_path`

## 게임 디렉터리에서 파일/폴더 열기 (`open_file_folder_in_game_dir`)
- **설명:** 운영체제의 기본 앱으로 파일 또는 폴더를 엽니다. 보안상의 이유로 대상은 게임 디렉터리 또는 기본 `.minecraft` 디렉터리 안에 있어야 합니다.
- **필수 값:** 예 - `target_path`

## 게임 디렉터리에 파일 쓰기 (`write_file_in_game_dir`)
- **설명:** 게임 디렉터리(인스턴스 루트)에 텍스트를 쓰거나 덧붙입니다. `.minecraft/` 접두사는 기본 런처 프로필을 대상으로 합니다(이 인스턴스와 다를 수 있음). 파일이 없으면 생성됩니다. 값 안의 `\n`으로 줄바꿈을 넣을 수 있으며, 추가 모드는 마지막 불리언으로 제어됩니다.
- **필수 값:** 예 - `path|||content|||append_bool`

## 시스템에서 파일 선택 (`select_file_to_game_dir`)
- **설명:** 기본 파일 선택 창(어떤 위치든 열 수 있음)을 열고 선택한 파일을 게임 디렉터리(인스턴스 루트)로 복사하거나, 접두사가 있을 경우 기본 `.minecraft/`로 복사합니다(해당 기본 경로는 이 인스턴스와 다를 수 있음). 확장자 필터, 사용자 지정 필터 레이블, 선택적 덮어쓰기 토글을 지원합니다.
- **필수 값:** 예 - 선택 구성

## 토스트 표시 (`show_toast`)
- **설명:** 설정 가능한 토스트 알림을 표시합니다
- **필수 값:** 예 - 토스트 설정

## 스케줄러 시작 (`start_scheduler`)
- **설명:** 스케줄러 ID로 스케줄러를 시작합니다.
- **필수 값:** 예 - `scheduler_id`

## 스케줄러 중지 (`stop_scheduler`)
- **설명:** 스케줄러 ID로 스케줄러를 중지합니다.
- **필수 값:** 예 - `scheduler_id`

## Minecraft 옵션 설정 (`edit_minecraft_option`)
- **설명:** Minecraft 설정 옵션을 수정합니다
- **필수 값:** 예 - `option_name:set_to_value`
