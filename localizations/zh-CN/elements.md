---
title: 元素
description: 了解 FancyMenu 的各种元素类型。
---

# 元素

元素是 FancyMenu 中自定义布局的构建块。你可以将它们添加到任何布局中，用于显示信息、增加交互性，或创建令人惊艳的视觉效果。

# 向布局添加元素

你可以在 **布局编辑器** 中为布局添加新元素。

1.  在编辑器背景上 **右键单击**，打开上下文菜单。
2.  将鼠标悬停在 **新元素** 上。
3.  会显示所有可用元素类型的列表。点击你想添加的类型。

![add_element](https://github.com/Keksuccino/FancyMenu/assets/35544624/865a66c5-76a6-404d-b746-d57802b1c12f)

添加元素后，你可以通过 **右键单击** 它来打开对应的上下文菜单，从而移动、调整大小并自定义它。要进一步了解如何排列元素，请参阅 [定位元素](https://docs.fancymenu.net/en/positioning-elements) 和 [元素标识符](https://docs.fancymenu.net/en/element-identifiers) 页面。

# 元素详解

以下列表包含 FancyMenu 中大部分（如果不是全部）可用元素。由于 FancyMenu 会更新，该列表有时可能会稍微过时。

## 按钮
一个可点击的按钮，可以执行各种各样的操作。这是创建交互式菜单时最强大、最灵活的元素之一。

*   **使用场景：**
    *   创建“加入 Discord”或“访问网站”按钮。
    *   为特定服务器添加快速加入按钮。
    *   构建不同菜单之间的自定义导航。
    *   创建用于切换其他布局显示/隐藏的按钮。
*   **主要特性：**
    *   **操作：** 可执行一系列动作，例如打开 URL、加入服务器、发送聊天命令、模拟另一个按钮的功能、控制变量等。详情请参阅 [动作脚本](https://docs.fancymenu.net/en/action-scripts) 文档。
    *   **自定义外观：** 正常、悬停和不可用状态的纹理都可完全自定义。支持透明背景、九宫切片、自定义标签颜色、悬停标签颜色、标签缩放、标签阴影开关以及按钮图标纹理。
    *   **声音：** 自定义点击、悬停和取消悬停声音。
    *   **模板模式：** 可作为模板，将其外观和属性应用到菜单中所有其他原版或模组按钮上，确保统一风格。更多内容请参阅 [按钮与滑块模板](https://docs.fancymenu.net/en/button-slider-templates) 页面。

## 滑块
一个可拖动的滑块，用户可通过它从列表或范围中选择一个值。其值变化时可以执行操作。

*   **使用场景：**
    *   创建自定义音量控制。
    *   用滑块切换不同主题或背景图片（使用“列表”类型）。
    *   调整某个特定的 Minecraft 选项，例如亮度或渲染距离。
*   **主要特性：**
    *   **类型：** 可以是 `值列表`（例如“简单”“普通”“困难”）、`整数范围`（例如 1-100）或 `小数范围`（例如 0.0-1.0）。
    *   **动态操作：** 在值变化时执行操作。滑块的当前值可在其操作中使用，以执行动态任务，并可与 [变量](https://docs.fancymenu.net/en/variables) 一起使用。
    *   **自定义：** 滑块标签可动态显示当前值。滑块把手和背景纹理都可完全自定义，包括透明背景、标签颜色/缩放选项、文字阴影开关以及自定义点击/取消悬停声音。

## 复选框
一个标准复选框，可切换开或关。被切换时可以执行操作。

*   **使用场景：**
    *   “我同意规则”复选框。
    *   在自定义菜单中启用或禁用某个特定功能的设置。
    *   切换某个布局或变量的开/关状态。
*   **主要特性：**
    *   **切换时执行操作：** 当状态变化时执行 [动作脚本](https://docs.fancymenu.net/en/action-scripts)。其当前状态（`true` 或 `false`）可在操作中访问。
    *   **变量模式：** 可直接绑定到 FancyMenu 变量，使复选框状态从该变量读取并写入该变量。
    *   **自定义外观：** 支持为背景（正常、悬停和不可用状态）以及勾选标记本身设置自定义纹理。

## 文本输入框
一个供用户输入文本的字段。其内容可绑定到 FancyMenu 变量，从而让你捕获并使用用户输入。

*   **使用场景：**
    *   与“加入服务器”按钮配合使用的“服务器 IP”输入框。
    *   输入玩家名称，用于自定义皮肤预览。
    *   创建一个基础的登录式界面。
*   **主要特性：**
    *   **变量绑定：** 用户输入的文本会存储到指定的 [变量](https://docs.fancymenu.net/en/variables) 中。
    *   **输入验证：** 可配置为仅接受特定字符类型，例如数字、URL 或纯文本。
    *   **最大长度：** 可设置输入的最大字符数限制。
    *   **外观和声音：** 支持自定义背景颜色、边框颜色、边框圆角、文字颜色、提示/占位文本、提示颜色、悬停声音、取消悬停声音和点击声音。

## 提示框
一个文本框，可配置为在特定位置显示，或跟随鼠标光标。其可见性通常由 [条件（加载要求）](https://docs.fancymenu.net/en/conditions) 控制。

*   **使用场景：**
    *   当用户悬停在按钮或图片上时显示详细信息。
    *   创建在特定条件下出现的上下文帮助提示。
    *   在光标旁显示动态信息（例如服务器状态）。
*   **主要特性：**
    *   **跟随鼠标：** 可设置为跟随鼠标指针。
    *   **支持 Markdown：** 提示框内容支持完整的 Markdown 格式。
    *   **自定义背景：** 背景可以是纯色，也可以是自定义九宫切片纹理，以实现完全主题化的外观。

## 物品
显示单个 Minecraft 物品，原版或模组物品都可以。

*   **使用场景：**
    *   将物品作为按钮或菜单选项的图标。
    *   创建商店或套装选择界面。
    *   显示玩家手持物品或盔甲。
*   **主要特性：**
    *   **自定义数据：** 可设置物品名称、Lore、数量、附魔光效，甚至自定义 NBT 数据。关于如何使用 NBT 的更多信息，请参阅 [NBT 数据占位符](https://docs.fancymenu.net/en/nbt-data-placeholder) 文档。
    *   **提示显示：** 可配置为悬停时显示物品的标准提示信息。

## 方块/物品 JSON 模型
从 Minecraft 资源或外部来源渲染一个方块或物品的 JSON 模型。

*   **使用场景：**
    *   在菜单中显示 3D 资源包模型。
    *   显示带有自定义纹理的物品/方块预览。
    *   构建基于模型的装饰性 UI 元素。
*   **主要特性：**
    *   **模型来源：** 可从 Minecraft 资源或外部来源加载模型 JSON。
    *   **纹理覆盖：** 支持设置自定义纹理。
    *   **渲染控制：** 包含旋转和光照控制。

## 图片
显示来自本地文件、网页 URL 或 Minecraft 资源位置的静态图片。

*   **使用场景：**
    *   添加服务器 Logo 或整合包品牌标识。
    *   创建装饰边框或 UI 框架。
    *   将图片作为更复杂 UI 设计的一部分。
*   **主要特性：**
    *   **九宫切片：** 可将图片作为可缩放的边框或面板使用，而不会拉伸变形角落。更多内容请参阅 [九宫切片与平铺](https://docs.fancymenu.net/en/nine-slicing-and-tiling) 页面。
    *   **纹理重复：** 图片可平铺以填充元素区域。
    *   **染色：** 可为图片应用颜色染色。
    *   **圆角：** 非九宫切片且非重复的图片可以设置圆角。
    *   **视差效果：** 可配置为随鼠标轻微移动，从而产生 3D 效果。更多内容请参阅 [视差效果](https://docs.fancymenu.net/en/parallax) 页面。

## 文本
一个用途非常广泛的文本显示元素。它既可以用于单行标签，也可以用于多页、可滚动文档。

*   **使用场景：**
    *   显示服务器规则、补丁说明或欢迎信息。
    *   使用 [占位符](https://docs.fancymenu.net/en/placeholders) 创建动态信息面板，例如：“欢迎，`{"placeholder":"playername"}`！”
    *   为你的 UI 添加标签和说明。
*   **主要特性：**
    *   **内容来源：** 文本可直接输入、从本地文件加载，或从网页 URL 获取。
    *   **支持 Markdown：** 支持广泛的 Markdown 富文本格式，包括标题、列表、代码块和表格。Markdown 元素的外观可完全自定义。更多信息请参阅 [文本格式化](https://docs.fancymenu.net/en/text-formatting) 页面。
    *   **滚动：** 当内容大于元素区域时会自动变为可滚动。滚动条可自定义或禁用。
    *   **样式：** 对文字颜色、缩放、对齐、阴影和行间距拥有完整控制。

## 视频
播放视频文件。非常适合电影式开场动画或装饰性循环背景。

> FancyMenu 3.9.0 中的新原生视频元素需要 **Watermedia V3** 和 **Watermedia Binaries V3**。旧的 **Video [MCEF]** 元素已被弃用。
{.is-warning}

*   **使用场景：**
    *   整合包或服务器宣传动画。
    *   用于菜单中的循环环境视频，让界面更有生命力。
    *   游戏内教程视频。
*   **主要特性：**
    *   **来源：** 支持本地视频文件和网页 URL。详情请参阅 [视频（MP4）](https://docs.fancymenu.net/en/video) 页面。
    *   **播放控制：** 可设置自动循环。音量、声音通道以及保持宽高比的行为都可调整。
    *   **交互控制：** 可通过按钮操作控制视频播放、跳转时间和音量。

## GLSL 着色器
在元素内部渲染自定义 GLSL 着色器。

*   **使用场景：**
    *   动画着色器面板。
    *   程序化视觉效果。
    *   将类似 Shadertoy 的菜单效果裁剪到元素矩形区域内。
*   **主要特性：**
    *   **着色器运行时：** 支持单通道和多通道着色器。
    *   **Shadertoy 支持：** 可使用 Shadertoy 风格的 `mainImage` 着色器。
    *   **Uniform：** 提供 FancyMenu 和输入 uniform。详情请参阅 [GLSL 着色器 API](https://docs.fancymenu.net/en/glsl-shader-api) 页面。

## 幻灯片放映
显示一系列图片。幻灯片放映的配置（图片、时间、过渡效果）是在位于 `/config/fancymenu/assets/slideshows/` 目录中的单独 `.properties` 文件中完成的。

*   **使用场景：**
    *   游戏内截图轮播展示。
    *   展示整合包的主要功能。
    *   作为在不同场景间循环切换的动态背景。
*   **主要特性：**
    *   加载预先配置好的幻灯片放映。有关设置说明，请参阅 [幻灯片放映](https://docs.fancymenu.net/en/slideshows) 文档。
    *   可设置为保持图片的宽高比。

## 矩形形状
一个简单的纯色矩形。

*   **使用场景：**
    *   在文本后方创建半透明背景，以提高可读性。
    *   设计简单的 UI 面板和分隔条。
    *   在布局设计期间作为彩色占位元素。
*   **主要特性：**
    *   支持 HEX RGBA 颜色、圆角以及可选模糊效果，使该形状可作为简单面板、染色层或模糊背景使用。

## 圆形形状
一个简单的纯色圆形/椭圆形。

*   **使用场景：**
    *   创建圆形点缀、指示器或柔和的 UI 区域。
    *   在不使用纹理文件的情况下构建主题化 UI 装饰。
*   **主要特性：**
    *   与矩形形状元素的工作方式类似，并支持颜色/模糊风格的视觉自定义。

## 闪屏文字
重现 Minecraft 标志性的黄色、弹跳式标题界面闪屏文字。

*   **使用场景：**
    *   用你自己的自定义消息替换原版闪屏文字。
    *   为任何菜单添加醒目、带动画的消息。
*   **主要特性：**
    *   **内容来源：** 可使用默认原版闪屏文案、自定义直接输入的文本列表，或来自本地文件的文本。
    *   **自定义：** 你可以切换弹跳效果，并自定义文字颜色、缩放、旋转和阴影。

## 玩家实体
在菜单中渲染一个玩家模型。

*   **使用场景：**
    *   在主菜单中显示当前玩家角色。
    *   创建队伍选择或职业预览界面。
    *   作为显示玩家皮肤和名称的“个人资料”区域。
*   **主要特性：**
    *   **动态外观：** 可配置为自动复制当前玩家的皮肤、披风和名称。更多相关内容请查看 [玩家头颅](https://docs.fancymenu.net/en/player-heads) 指南。
    *   **自定义姿势：** 可精细控制头部、身体、手臂和腿部的旋转。头部和身体也可以设置为跟随鼠标光标。
    *   **属性：** 可设置为幼年、蹲伏，或使用苗条模型。

## 浏览器
一个在游戏内渲染实时网页的元素。

此元素需要安装并正常工作 **MCEF（Minecraft Chromium Embedded Framework）** 模组！

你可以从 [CurseForge](https://www.curseforge.com/minecraft/mc-mods/mcef) 和 [Modrinth](https://modrinth.com/mod/mcef) 的官方项目页面下载 MCEF。

对于较新的 Minecraft 版本（1.21.5+），官方 MCEF 项目不提供构建版本，但有一个面向最新 Minecraft 版本的分支版本，可在 [这里](https://www.curseforge.com/minecraft/mc-mods/mcef-keksuccino)（CurseForge）和 [这里](https://modrinth.com/mod/mcef-keksuccino)（Modrinth）找到。这个分支由 Keksuccino 维护，以便尽快为最新 Minecraft 版本提供构建。

*   **使用场景：**
    *   显示服务器的实时 Dynmap。
    *   嵌入 YouTube 视频播放器。
    *   直接在游戏内显示 wiki 或文档页面。
*   **主要特性：**
    *   **交互性：** 可设置为完全可交互，允许用户点击链接、滚动和输入文字。
    *   **媒体控制：** 提供静音媒体、循环播放视频以及隐藏所加载页面视频控制项的选项。

### 加载本地 HTML 文件
Browser 元素允许你加载 `/config/fancymenu/assets/` 中的本地 HTML 文档！这意味着你可以显示本地由浏览器渲染的内容，例如精美的更新日志等。

要加载本地 HTML 文件，请以 `file:///` 开头输入 URL，后面跟上**短**文件路径，例如 `/config/fancymenu/assets/cool_changelog.html`，最终效果如下：`file:///config/fancymenu/assets/cool_changelog.html`。

在 **Linux** 上，你需要提供绝对文件路径，但由于硬编码绝对路径会破坏布局，因此你需要让一个占位符动态地将短路径转换为绝对路径：`file:///{"placeholder":"absolute_path","values":{"short_path":"/config/fancymenu/assets/cool_changelog.html"}}`。

非常重要：在 Linux 上，短路径必须像上面的示例一样以 `/` 开头。否则它将无法工作。

## 元素动画器
一个用于创建复杂、基于关键帧动画的强大工具。它可以对一个或多个其他元素的位置、大小和锚点进行动画处理。

*   **使用场景：**
    *   创建精致的开场动画，让菜单元素滑入或淡入视图。
    *   让装饰元素脉动、旋转或沿路径移动。
    *   让通知弹出后再消失。
*   **主要特性：**
    *   **关键帧编辑器：** 一个专门用于在时间线上添加、编辑和排列关键帧的编辑器。
    *   **多目标：** 单个动画器可同时控制多个“目标”元素。
    *   **控制：** 动画可设置为循环。你也可以只选择对位置或大小进行动画处理。
    *   **时间偏移：** 目标元素可使用各自独立或随机的起始时间偏移。
    *   **[进一步了解元素动画器。](https://docs.fancymenu.net/en/element-animator)**

## 计时器
一个不可见元素，会按固定间隔（每个“tick”）执行一系列操作。

> 对于 FancyMenu 3.9.0+ 中新的后台自动化功能，建议使用 [调度器](https://docs.fancymenu.net/en/schedulers)。调度器是全局的、更易组织，并且可以独立于特定界面持续运行。
{.is-info}

*   **使用场景：**
    *   定期检查服务器在线状态并更新文本元素。
    *   创建一个会更新文本标签的倒计时。
    *   重复运行脚本以创建自定义行为。
*   **主要特性：**
    *   **时间控制：** 可设置 tick 之间的延迟，单位为毫秒。
    *   **Tick 模式：** 可设置为持续 tick、每个游戏会话仅一次，或每次加载菜单时一次。
    *   **异步：** 可在单独线程中运行其 [动作](https://docs.fancymenu.net/en/action-scripts)，以避免影响游戏性能，不过某些动作不能这样运行。

## 音频
一个不可见元素，用于播放音频文件。它可以管理播放列表，并提供多种播放控制。

*   **使用场景：**
    *   为菜单添加自定义背景音乐。
    *   创建一个带有播放控制按钮的音乐播放器（上一首/下一首、音量）。
    *   播放环境氛围音。
*   **主要特性：**
    *   **播放列表：** 可管理多个音轨。
    *   **播放模式：** 可按顺序播放或随机播放（并支持音轨权重，使某些曲目出现得更频繁）。
    *   **控制：** 支持循环、音量调整，并可分配到特定声音通道（例如 Master、Music）。更多信息请参阅 [菜单背景音乐](https://docs.fancymenu.net/en/background-music) 页面。

## 音乐控制器
一个不可见元素，用于在特定菜单中控制 Minecraft 默认音乐的播放。

*   **使用场景：**
    *   在你想通过 **音频** 元素播放自定义音乐的界面中，禁用默认菜单音乐。
    *   在游戏内打开菜单时，停止游戏世界中的音乐继续播放。
*   **主要特性：**
    *   分别提供开关，用于控制原版“菜单音乐”和“世界音乐”。

## 进度条
一个可自定义的条形元素，用于直观表示数值。

*   **使用场景：**
    *   使用 `{"placeholder":"world_load_progress"}` 跟踪世界加载进度的加载条。
    *   用于游戏内 HUD 的生命值、饥饿值或经验值条。
    *   由 **滑块** 元素控制的音量指示条。
*   **主要特性：**
    *   **动态值：** 进度值（0-100 或 0.0-1.0）通过支持 [占位符](https://docs.fancymenu.net/en/placeholders) 的文本字段设置。
    *   **外观：** 条的方向（上、下、左、右）、颜色、纹理以及条/背景纹理的九宫切片都可自定义。
    *   **动画：** 具有平滑填充动画，使进度变化看起来不那么突兀。

## 拖拽器
一个不可见元素，用户可以点击并拖动它来移动位置。其他元素可以锚定到它上面，以创建可移动的小部件。

*   **使用场景：**
    *   创建可拖动的时钟或信息面板。
    *   允许用户按自己的偏好自定义 UI 元素的位置。
*   **主要特性：**
    *   **持久位置：** 被拖动后的偏移会被保存，因此即使重启游戏，该元素也会停留在用户最后放置的位置。
    *   **锚点：** 作为其他元素的可移动锚点，这是 [定位元素](https://docs.fancymenu.net/en/positioning-elements) 的关键组成部分。

## 光标
一个不可见元素，在布局激活时，用自定义图片替换默认系统光标。

*   **使用场景：**
    *   创建与整合包美学相匹配的完整主题化 UI。
*   **主要特性：**
    *   **自定义纹理：** 光标可使用任意图片。
    *   **热点：** 你可以定义图片中作为“点击点”的精确像素。更多内容请参阅 [自定义光标](https://docs.fancymenu.net/en/custom-cursor) 指南。
