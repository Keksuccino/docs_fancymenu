---
title: 元素
description: 了解 FancyMenu 的各种元素类型。
---
# 元素

元素是 FancyMenu 中自定义布局的构建块。你可以将它们添加到任何布局中，用于显示信息、增加交互性，或创建令人惊艳的视觉效果。

# 将元素添加到布局

你可以在 **布局编辑器** 中为你的布局添加新元素。

1.  在编辑器背景上**右键单击**，打开上下文菜单。
2.  将鼠标悬停在 **新建元素** 上。
3.  将会显示所有可用元素类型的列表。点击你想要添加的那个。

![add_element](https://github.com/Keksuccino/FancyMenu/assets/35544624/865a66c5-76a6-404d-b746-d57802b1c12f)

添加元素后，你可以通过**右键单击**它来打开其专属上下文菜单，从而移动、调整大小并自定义它。要进一步了解如何排列元素，请参阅 [定位元素](./positioning-elements) 和 [元素标识符](./element-identifiers)。

# 详细元素说明

本节列出了 FancyMenu 内置的元素。使用 [层与组](./layers-and-groups) 来组织它们的渲染顺序。

## 按钮
一种可点击按钮，可以执行各种各样的操作。这是用于创建交互式菜单最强大、最灵活的元素之一。

*   **使用场景：**
    *   创建“加入 Discord”或“访问网站”按钮。
    *   为特定服务器添加快速加入按钮。
    *   构建不同菜单之间的自定义导航。
    *   创建用于切换其他布局开关的按钮。
*   **主要特性：**
    *   **动作：** 可执行一系列 [动作](./action-scripts)，例如打开 URL、加入服务器、发送聊天命令、模拟另一个按钮的功能，或控制变量。
    *   **自定义外观：** 可为正常、悬停和不可用状态提供完整可自定义的纹理。支持透明背景、九宫格切片、自定义标签颜色、悬停标签颜色、标签缩放、标签阴影开关以及按钮图标纹理。
    *   **声音：** 自定义点击、悬停和取消悬停声音。
    *   **模板模式：** 可将其外观和属性应用到菜单中的其他原版或模组按钮上。参见 [按钮与滑块模板](./button-slider-templates)。
    *   **自动化原版/模组组件点击：** 现有的原版和模组组件具有 **自动点击** 属性，可在屏幕加载时按指定次数触发其原始点击行为。详情参见 [原版元素](./vanilla-elements#automated-clicks)。

## 滑块
一种可拖动的滑块，用户可以通过它从列表或范围中选择一个值。值发生变化时，它可以执行动作。

*   **使用场景：**
    *   创建自定义音量控制。
    *   使用“列表”类型在不同主题或背景图像之间切换的滑块。
    *   调整某个特定的 Minecraft 选项，例如亮度或渲染距离。
*   **主要特性：**
    *   **类型：** 可为 `Value List`（例如“简单”“普通”“困难”）、`Integer Range`（例如 1-100）或 `Decimal Range`（例如 0.0-1.0）。
    *   **动态动作：** 当值变化时执行动作。当前值可与 [变量](./variables) 一起使用。
    *   **自定义：** 滑块标签可动态显示当前值。滑块把手和背景纹理完全可自定义，包括透明背景、标签颜色/缩放选项、文字阴影开关以及自定义点击/取消悬停声音。

## 复选框
一个标准复选框，可切换开或关。被切换时可以执行动作。

*   **使用场景：**
    *   “我同意规则”复选框。
    *   在你的自定义菜单中启用或禁用某个特定功能的设置。
    *   切换某个布局或变量的开/关。
*   **主要特性：**
    *   **切换时执行动作：** 当状态改变时执行 [动作脚本](./action-scripts)。当前状态（`true` 或 `false`）可供其动作使用。
    *   **变量模式：** 可直接链接到 FancyMenu 变量，使复选框状态从该变量读取并写入该变量。
    *   **持久状态：** 当禁用变量模式时，复选框会按元素标识符自动保存其状态，并在重启游戏后恢复。这些状态存储在 `<game-directory>/checkbox_states.json` 中。在变量模式下，所链接的 FancyMenu 变量就是复选框的状态来源。
    *   **自定义外观：** 支持为背景（正常、悬停和不可用状态）以及勾选标记本身设置自定义纹理。

## 文本输入框
用户可以在其中输入文本的字段。其内容可以链接到 FancyMenu 变量，从而让你捕获并使用用户输入。

*   **使用场景：**
    *   与“加入服务器”按钮配合使用的“服务器 IP”输入框。
    *   用于输入玩家名称，以进行自定义皮肤预览的字段。
    *   创建一个基础的登录式界面。
*   **主要特性：**
    *   **变量链接：** 将输入的文本存储到指定 [变量](./variables) 中。
    *   **输入验证：** 可配置为仅接受特定字符类型，例如数字、URL 或纯文本。
    *   **最大长度：** 可为输入设置最大字符限制。
    *   **外观和声音：** 支持自定义背景颜色、边框颜色、边框圆角、文本颜色、提示/占位文本、提示颜色、悬停声音、取消悬停声音和点击声音。

## 工具提示
一个可固定在某个位置或跟随鼠标光标的文本框。其可见性通常由 [加载条件](./conditions) 控制。

*   **使用场景：**
    *   当用户悬停在按钮或图片上时显示详细信息。
    *   创建在特定条件下出现的上下文帮助提示。
    *   在光标旁显示动态信息（如服务器状态）。
*   **主要特性：**
    *   **跟随鼠标：** 可设置为跟随鼠标指针。
    *   **Markdown 支持：** 工具提示内容支持完整的 Markdown 格式。
    *   **自定义背景：** 背景可以是纯色，也可以是自定义九宫格切片纹理，以实现完整的主题化外观。

## 物品
显示一个单独的 Minecraft 物品，可以是原版或模组物品。

*   **使用场景：**
    *   将物品用作按钮或菜单选项的图标。
    *   创建商店或套装选择 GUI。
    *   显示玩家手持的物品或盔甲。
*   **主要特性：**
    *   **自定义数据：** 支持自定义名称、Lore、数量、附魔光效以及 NBT 数据。参见 [NBT 数据占位符](./nbt-data-placeholder)。
    *   **工具提示显示：** 可配置为在悬停时显示物品的标准工具提示。

## 方块/物品 JSON 模型
从 Minecraft 资源或外部来源渲染一个方块或物品 JSON 模型。

*   **使用场景：**
    *   在菜单中显示资源包的 3D 模型。
    *   显示带有自定义纹理的物品/方块预览。
    *   构建基于模型的装饰性 UI 元素。
*   **主要特性：**
    *   **模型来源：** 可从 Minecraft 资源或外部来源加载模型 JSON。
    *   **纹理覆盖：** 支持设置自定义纹理。
    *   **渲染控制：** 模型偏移、缩放、三轴旋转、半透明渲染以及模型 GUI 变换。
    *   **照明：** 两个可配置光源，具有独立的色相和旋转控制。

## 图片
显示来自本地文件、网页 URL 或 Minecraft 资源位置的静态图片。

*   **使用场景：**
    *   添加服务器徽标或整合包品牌标识。
    *   创建装饰性边框或 UI 框架。
    *   将图片作为更复杂 UI 设计的一部分。
*   **主要特性：**
    *   **九宫格切片：** 缩放边框或面板时不会扭曲角落。参见 [九宫格切片与平铺](./nine-slicing-and-tiling)。
    *   **纹理重复：** 图片可以平铺以填充元素区域。
    *   **染色：** 可以为图片应用颜色染色。
    *   **圆角：** 非九宫格切片且非重复的图片可以有圆角。
    *   **视差效果：** 随鼠标移动以营造视觉深度。参见 [视差效果](./parallax)。

## 文本
一个功能极其丰富的文本显示元素。它既可以用于单行标签，也可以用于多页、可滚动文档。

*   **使用场景：**
    *   显示服务器规则、更新说明或欢迎消息。
    *   使用 [占位符](./placeholders) 创建动态信息面板，例如 `Welcome, {"placeholder":"playername"}!`。
    *   为你的 UI 添加标签和说明文本。
*   **主要特性：**
    *   **内容来源：** 文本可直接输入、从本地文件加载，或从网页 URL 获取。
    *   **Markdown 支持：** 支持标题、列表、代码块、表格和其他 Markdown 格式。参见 [文本格式化](./text-formatting)。
    *   **滚动：** 当内容大于元素区域时会自动变为可滚动。滚动条可自定义或禁用。
    *   **样式：** 可完全控制文本颜色、缩放、对齐、阴影和行间距。

## 视频
播放一个视频文件。这非常适合用于电影式开场或装饰性循环背景。

> [!WARNING]
> 原生视频元素需要 **Watermedia V3** 和 **Watermedia Binaries V3**。旧的 **Video [Rinku]** 元素已弃用。

*   **使用场景：**
    *   展示整合包或服务器的动画预告片。
    *   循环播放环境视频，为你的菜单增添生气。
    *   游戏内教程视频。
*   **主要特性：**
    *   **来源：** 支持本地视频文件和网页 URL。参见 [视频](./video)。
    *   **播放控制：** 可设置为自动循环。其音量、声音通道和保持宽高比的行为均可调整。
    *   **交互控制：** 可通过按钮动作控制视频播放、跳转时间点和音量。

## GLSL 着色器
在元素内部渲染自定义 GLSL 着色器。

*   **使用场景：**
    *   动画着色器面板。
    *   程序化视觉效果。
    *   裁剪到元素矩形范围内的 Shadertoy 风格菜单效果。
*   **主要特性：**
    *   **着色器运行时：** 支持单通道和多通道着色器。
    *   **Shadertoy 支持：** 可使用 Shadertoy 风格的 `mainImage` 着色器。
    *   **Uniforms：** 暴露 FancyMenu 和输入 uniforms。参见 [GLSL 着色器 API](./glsl-shader-api)。

## 幻灯片放映
显示一系列图片。其图片和 `properties.txt` 配置文件位于 `<game-directory>/config/fancymenu/slideshows/` 下该幻灯片放映自己的子目录中。

*   **使用场景：**
    *   循环展示游戏内截图画廊。
    *   展示整合包的关键特性。
    *   作为会在不同场景间切换的动态背景。
*   **主要特性：**
    *   加载预配置的 [幻灯片放映](./slideshows)。
    *   可设置为保持图片的宽高比。

## 矩形形状
一个简单的纯色矩形。

*   **使用场景：**
    *   在文本后方创建半透明背景，以提升可读性。
    *   设计简单的 UI 面板和分隔线。
    *   在布局设计期间作为有颜色的占位元素。
*   **主要特性：**
    *   支持 HEX RGBA 颜色、圆角和可选模糊效果，使该形状可作为简单面板、染色层或模糊背景使用。

## 圆形形状
一个简单的纯色圆形/椭圆形状。

*   **使用场景：**
    *   创建圆形点缀、指示器或柔和的 UI 区域。
    *   在不使用纹理文件的情况下构建主题化 UI 装饰。
*   **主要特性：**
    *   支持颜色、模糊，以及可配置的圆度/指数值。

## 闪烁文字
重现 Minecraft 标题界面中标志性的黄色弹跳闪烁文字。

*   **使用场景：**
    *   用你自己的自定义消息替换原版闪烁文字。
    *   为任何菜单添加一个醒目、带动画的消息。
*   **主要特性：**
    *   **内容来源：** 可使用默认的原版闪烁文字、直接输入的自定义文本列表，或来自本地文件的文本。
    *   **自定义：** 可切换弹跳效果，并自定义文本颜色、缩放、旋转和阴影。

## 玩家实体
在菜单中渲染玩家模型。

*   **使用场景：**
    *   在主菜单中显示当前玩家角色。
    *   创建队伍选择或职业预览界面。
    *   一个显示玩家皮肤和名称的“个人资料”区域。
*   **主要特性：**
    *   **动态外观：** 可复制当前玩家的皮肤、披风和名称。参见 [玩家头颅](./player-heads)。
    *   **自定义姿势：** 可精细控制头部、身体、手臂和腿部的旋转。头部和身体也可设置为跟随鼠标光标。
    *   **属性：** 可设置为幼年、蹲伏，或使用纤细模型。

## 浏览器
一种在游戏内渲染实时网页的元素。

此元素需要安装并正常工作 **[Rinku](https://modrinth.com/mod/rinku)** 模组！

你可以从 [CurseForge](https://www.curseforge.com/minecraft/mc-mods/rinku) 和 [Modrinth](https://modrinth.com/mod/rinku) 的官方项目页面下载 Rinku。

*   **使用场景：**
    *   显示服务器的实时 Dynmap。
    *   嵌入 YouTube 视频播放器。
    *   直接在游戏内显示维基或文档页面。
*   **主要特性：**
    *   **交互性：** 可设为完全可交互，允许用户点击链接、滚动和输入。
    *   **媒体控制：** 提供静音媒体、循环视频和隐藏所加载页面上的视频控件等选项。

### 加载本地 HTML 文件
浏览器元素可以从 `<game-directory>/config/fancymenu/assets/` 加载本地 HTML 文档。

要加载本地 HTML 文件，请以 `file:///` 开头输入 URL，后接 SHORT 文件路径，例如 `/config/fancymenu/assets/cool_changelog.html`，这样最终看起来就像这样：`file:///config/fancymenu/assets/cool_changelog.html`。

在 **Linux** 上，请使用 [**绝对文件/文件夹路径** 占位符](./placeholders#absolute-filefolder-path-absolute_path)，而不是硬编码某个实例专用的绝对路径：`file:///{"placeholder":"absolute_path","values":{"short_path":"/config/fancymenu/assets/cool_changelog.html"}}`。

Linux 的短路径必须以 `/` 开头，如示例所示。

## 元素动画器
一个用于创建复杂、基于关键帧动画的强大工具。它可以为一个或多个其他元素的位置信息、大小和锚点进行动画化。

*   **使用场景：**
    *   让元素滑入或滑出视野。
    *   调整面板或通知的大小。
    *   对位置偏移和锚点过渡进行动画处理。
*   **主要特性：**
    *   **关键帧编辑器：** 一个专用编辑器，可在时间轴上添加、编辑和排列关键帧。
    *   **多目标：** 一个动画器可同时控制多个“目标”元素。
    *   **控制：** 动画可设置为循环。你也可以选择仅为位置或大小制作动画。
    *   **时间偏移：** 目标元素可使用单独或随机的开始时间偏移。
    *   参见 [元素动画器](./element-animator) 了解设置和关键帧编辑。

## 计时器
一个不可见的元素，会按固定间隔（每个“tick”）执行一组动作。

> [!NOTE]
> 对于后台自动化，建议使用 [调度器](./schedulers)。调度器是全局的，可独立于特定屏幕运行。

*   **使用场景：**
    *   定期检查服务器在线状态并更新文本元素。
    *   创建一个会更新文本标签的倒计时计时器。
    *   重复运行脚本以创建自定义行为。
*   **主要特性：**
    *   **时间控制：** 你可以设置 tick 之间的延迟，单位为毫秒。
    *   **Tick 模式：** 可设置为持续 tick、每个游戏会话仅一次，或每次菜单加载时仅一次。
    *   **异步：** 可单独运行其 [动作](./action-scripts)，不过启用此选项时某些动作无法运行。

## 音频
一个不可见的元素，用于播放音频文件。它可以管理播放列表并提供各种播放控制。

*   **使用场景：**
    *   为菜单添加自定义背景音乐。
    *   创建一个带有按钮控制播放的音乐播放器（下一首/上一首、音量）。
    *   播放环境音景。
*   **主要特性：**
    *   **播放列表：** 可管理多个音轨。
    *   **播放模式：** 可按顺序播放音轨，也可随机播放（支持轨道权重，使某些音轨比其他音轨更常出现）。
    *   **控制：** 支持循环、音量调节和声音通道选择。参见 [菜单背景音乐](./background-music)。

## 音乐控制器
一个不可见的元素，用于在特定菜单中控制 Minecraft 的默认音乐播放。

*   **使用场景：**
    *   在你想通过 [**音频** 元素](#audio) 播放自己的自定义音乐的界面上，禁用默认菜单音乐。
    *   在游戏内打开菜单时，停止游戏世界音乐继续播放。
*   **主要特性：**
    *   提供独立开关来控制原版的“菜单音乐”和“世界音乐”。

## 进度条
一个可自定义的条形元素，用于直观表示数值。

*   **使用场景：**
    *   使用 `{"placeholder":"world_load_progress"}` 跟踪世界加载进度的加载条。
    *   用于游戏内 HUD 的生命值、饥饿值或经验值进度条。
    *   由 [**滑块** 元素](#slider) 控制的音量指示器。
*   **主要特性：**
    *   **动态值：** 进度值（0-100 或 0.0-1.0）通过支持 [占位符](./placeholders) 的文本字段设置。
    *   **外观：** 条形方向（上、下、左、右）、颜色、纹理以及条形/背景纹理的九宫格切片均可自定义。
    *   **动画：** 具备平滑填充动画，使进度变化看起来不那么突兀。
    *   **基于进度的元素锚点：** 当另一个元素将进度条作为其 **元素** 锚点时，启用 **将进度用于元素锚点** 可将该锚点移动到已填充区域当前边缘。这样，锚定元素会随着进度条的进度一起移动，而不是始终附着在进度条静态边界上。

## 拖拽器
一个不可见的元素，用户可以点击并拖动它来移动。其他元素可以锚定到它上面，从而创建可移动的组件。

*   **使用场景：**
    *   创建可拖动的时钟或信息面板。
    *   允许用户按自己的偏好自定义 UI 元素位置。
*   **主要特性：**
    *   **可选持久化：** 启用 **保存用户拖拽偏移** 可在屏幕重新打开和游戏重启后保留用户拖拽后的位移。禁用则会重置该偏移。
    *   **锚点：** 作为其他元素的可移动锚点，这是 [定位元素](./positioning-elements) 的关键部分。

## 光标
一个不可见的元素，当布局处于激活状态时，会用自定义图片替换默认系统光标。

*   **使用场景：**
    *   创建与整合包美术风格相匹配的完整主题化 UI。
*   **主要特性：**
    *   **自定义纹理：** 可将任何图片用作光标。
    *   **热点：** 设置用于点击位置的精确图像像素。参见 [自定义光标](./custom-cursor)。
