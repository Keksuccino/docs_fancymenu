---
title: 元素
description: 关于 FancyMenu 元素类型的一切知识。
---
# 元素

元素是 FancyMenu 中自定义布局的构建模块。你可以将它们添加到任何布局中，用于显示信息、增加交互性，或创建令人惊艳的视觉效果。

# 向布局中添加元素

你可以在 **布局编辑器** 中为布局添加新元素。

1.  在编辑器背景上**右键单击**，打开上下文菜单。
2.  将鼠标悬停在 **New Element** 上。
3.  会出现所有可用元素类型的列表。单击你想添加的那个。

![add_element](https://github.com/Keksuccino/FancyMenu/assets/35544624/865a66c5-76a6-404d-b746-d57802b1c12f)

添加元素后，你可以通过在它上面**右键单击**来打开其专属上下文菜单，从而移动、调整大小并自定义它。要进一步了解如何排列元素，请参阅 [定位元素](./positioning-elements) 和 [元素标识符](./element-identifiers)。

# 详细元素说明

本节列出了 FancyMenu 的内置元素。使用 [图层和组](./layers-and-groups) 来组织它们的渲染顺序。

## 按钮
可点击的按钮，可以执行各种各样的操作。这是创建交互式菜单时最强大、最灵活的元素之一。

*   **使用场景：**
    *   创建“加入 Discord”或“访问网站”按钮。
    *   为特定服务器添加快速加入按钮。
    *   构建不同菜单之间的自定义导航。
    *   创建可切换其他布局启用或禁用的按钮。
*   **主要功能：**
    *   **操作：** 可以执行一系列 [动作](./action-scripts)，例如打开 URL、加入服务器、发送聊天命令、模拟另一个按钮的功能，或控制变量。
    *   **自定义外观：** 正常、悬停和禁用状态的纹理都可完全自定义。支持透明背景、九宫切片、自定义标签颜色、悬停标签颜色、标签缩放、标签阴影开关以及按钮图标纹理。
    *   **声音：** 可自定义点击、悬停和取消悬停声音。
    *   **模板模式：** 可将其外观和属性应用到菜单中的其他原版或模组按钮。详见 [按钮与滑块模板](./button-slider-templates)。
    *   **自动原版/模组控件点击：** 现有的原版和模组控件都有一个 **Automated Clicks** 属性，可在屏幕加载时按指定次数调用其原始点击行为。详见 [原版元素](./vanilla-elements#automated-clicks)。

## 滑块
用户可以拖动的滑块，用于从列表或范围中选择一个值。其值变化时可以执行操作。

*   **使用场景：**
    *   创建自定义音量控制。
    *   用于在不同主题或背景图之间切换的滑块（使用“列表”类型）。
    *   调整某个 Minecraft 选项，例如亮度或渲染距离。
*   **主要功能：**
    *   **类型：** 可以是 `值列表`（例如“简单”“普通”“困难”）、`整数范围`（例如 1-100）或 `小数范围`（例如 0.0-1.0）。
    *   **动态动作：** 当其值变化时执行操作。当前值可与 [变量](./variables) 一起使用。
    *   **自定义：** 滑块标签可动态显示当前值。滑块把手和背景纹理都可以完全自定义，包括透明背景、标签颜色/缩放选项、文字阴影开关以及自定义点击/取消悬停声音。

## 复选框
标准复选框，可切换开启或关闭。切换时可以执行操作。

*   **使用场景：**
    *   “我同意规则”复选框。
    *   在自定义菜单中启用或禁用某个特性的设置。
    *   切换某个布局或变量的开/关状态。
*   **主要功能：**
    *   **切换时执行操作：** 状态改变时执行 [动作脚本](./action-scripts)。当前状态（`true` 或 `false`）可供其动作使用。
    *   **变量模式：** 可直接链接到 FancyMenu 变量，使复选框状态从该变量读取并写入该变量。
    *   **持久化状态：** 当禁用变量模式时，复选框会按元素标识符自动保存其状态，并在重启游戏后恢复。这些状态存储在 `<game-directory>/checkbox_states.json` 中。在变量模式下，链接的 FancyMenu 变量将作为复选框状态来源。
    *   **自定义外观：** 支持背景的自定义纹理（正常、悬停和禁用状态）以及勾选标记本身的自定义纹理。

## 文本输入框
用户可以输入文本的字段。其内容可以链接到 FancyMenu 变量，从而让你捕获并使用用户输入。

*   **使用场景：**
    *   与“加入服务器”按钮配合使用的“服务器 IP”输入框。
    *   输入玩家名称，用于自定义皮肤预览。
    *   创建一个基础的登录式界面。
*   **主要功能：**
    *   **变量绑定：** 将输入的文本存储到指定的 [变量](./variables) 中。
    *   **输入验证：** 可配置为仅接受特定字符类型，例如数字、URL 或纯文本。
    *   **最大长度：** 可设置输入的最大字符数限制。
    *   **外观和声音：** 支持自定义背景颜色、边框颜色、边框圆角、文本颜色、提示/占位文本、提示颜色、悬停声音、取消悬停声音和点击声音。

## 工具提示
可出现在固定位置或跟随鼠标光标的文本框。其可见性通常由 [加载条件](./conditions) 控制。

*   **使用场景：**
    *   当用户悬停在按钮或图片上时显示详细信息。
    *   创建在特定条件下显示的情境帮助提示。
    *   在光标旁显示动态信息（例如服务器状态）。
*   **主要功能：**
    *   **跟随鼠标：** 可设置为跟随鼠标指针。
    *   **Markdown 支持：** 工具提示内容支持完整的 Markdown 格式。
    *   **自定义背景：** 背景可以是纯色，也可以是自定义九宫切片纹理，以实现完整主题化外观。

## 物品
显示一个 Minecraft 物品，可以来自原版或模组。

*   **使用场景：**
    *   使用物品作为按钮或菜单选项的图标。
    *   创建商店或套装选择 GUI。
    *   显示玩家手持物品或盔甲。
*   **主要功能：**
    *   **自定义数据：** 支持自定义名称、Lore、数量、附魔闪光效果和 NBT 数据。详见 [NBT 数据占位符](./nbt-data-placeholder)。
    *   **工具提示显示：** 可配置为在悬停时显示物品的标准工具提示。

## 方块/物品 JSON 模型
从 Minecraft 资源或外部来源渲染方块或物品的 JSON 模型。

*   **使用场景：**
    *   在菜单中显示 3D 资源包模型。
    *   展示带有自定义纹理的物品/方块预览。
    *   构建基于模型的装饰性 UI 元素。
*   **主要功能：**
    *   **模型来源：** 可从 Minecraft 资源或外部来源加载模型 JSON。
    *   **纹理覆盖：** 支持设置自定义纹理。
    *   **渲染控制：** 模型偏移、缩放、三轴旋转、半透明渲染以及模型 GUI 变换。
    *   **光照：** 两个可配置光源，分别具有独立的色相和旋转控制。

## 图片
显示来自本地文件、网络 URL 或 Minecraft 资源位置的静态图片。

*   **使用场景：**
    *   添加服务器徽标或整合包品牌标识。
    *   创建装饰边框或 UI 画框。
    *   将图片作为更复杂 UI 设计的一部分。
*   **主要功能：**
    *   **九宫切片：** 缩放边框或面板时不会扭曲角落。详见 [九宫切片与平铺](./nine-slicing-and-tiling)。
    *   **纹理重复：** 图片可以平铺以填充元素区域。
    *   **染色：** 可对图片应用颜色染色。
    *   **圆角：** 非九宫切片且非重复的图片可以拥有圆角。
    *   **视差效果：** 随鼠标移动，营造视觉深度。详见 [视差效果](./parallax)。

## 文本
一个用途极其广泛的文本显示元素。它既可用于单行标签，也可用于多页、可滚动文档。

*   **使用场景：**
    *   显示服务器规则、更新日志或欢迎信息。
    *   使用 [占位符](./placeholders) 创建动态信息面板，例如 `Welcome, {"placeholder":"playername"}!`。
    *   为你的界面添加标签和说明。
*   **主要功能：**
    *   **内容来源：** 文本可直接输入、从本地文件加载，或从网页 URL 获取。
    *   **Markdown 支持：** 支持标题、列表、代码块、表格和其他 Markdown 格式。详见 [文本格式化](./text-formatting)。
    *   **滚动：** 当内容大于元素区域时会自动变为可滚动。滚动条可以自定义或禁用。
    *   **样式：** 可完全控制文本颜色、缩放、对齐、阴影和行间距。

## 视频
播放视频文件。非常适合电影式开场或装饰性的循环背景。

> [!WARNING]
> 原生视频元素需要 **Watermedia V3** 和 **Watermedia Binaries V3**。旧的 **Video [MCEF]** 元素已被弃用。

*   **使用场景：**
    *   动画化的整合包或服务器预告片。
    *   循环播放的环境视频，为菜单增添活力。
    *   游戏内教程视频。
*   **主要功能：**
    *   **来源：** 支持本地视频文件和网页 URL。详见 [视频](./video)。
    *   **播放控制：** 可设置为自动循环。音量、声音频道和保持宽高比行为均可调整。
    *   **交互控制：** 可通过按钮动作控制视频播放、跳转时间和音量。

## GLSL 着色器
在元素内渲染自定义 GLSL 着色器。

*   **使用场景：**
    *   动画着色器面板。
    *   程序化视觉效果。
    *   裁切到元素矩形区域内的 Shadertoy 风格菜单效果。
*   **主要功能：**
    *   **着色器运行时：** 支持单通道和多通道着色器。
    *   **Shadertoy 支持：** 可使用 Shadertoy 风格的 `mainImage` 着色器。
    *   **Uniform：** 提供 FancyMenu 和输入 uniform。详见 [GLSL 着色器 API](./glsl-shader-api)。

## 幻灯片
显示一系列图片。其图片和 `properties.txt` 配置文件位于 `<game-directory>/config/fancymenu/slideshows/` 下幻灯片自己的子目录中。

*   **使用场景：**
    *   轮播游戏内截图集。
    *   展示整合包的关键特性。
    *   通过不同场景循环切换来制作动态背景。
*   **主要功能：**
    *   加载预配置的 [幻灯片](./slideshows)。
    *   可设置为保持图片的宽高比。

## 矩形形状
一个简单的纯色矩形。

*   **使用场景：**
    *   在文本后创建半透明背景以提升可读性。
    *   设计简单的 UI 面板和分隔线。
    *   在布局设计阶段作为彩色占位符。
*   **主要功能：**
    *   支持 HEX RGBA 颜色、圆角和可选模糊，使该形状可作为简单面板、染色层或模糊背景使用。

## 圆形形状
一个简单的纯色圆形/椭圆形。

*   **使用场景：**
    *   创建圆形装饰、指示器或柔和的 UI 区域。
    *   无需纹理文件即可构建主题化 UI 装饰。
*   **主要功能：**
    *   支持颜色、模糊，以及可配置的圆润度/指数值。

## 闪屏文字
重现 Minecraft 标题界面中标志性的黄色弹跳闪屏文字。

*   **使用场景：**
    *   用你自己的自定义消息替换原版闪屏文字。
    *   为任何菜单添加醒目、动画化的信息。
*   **主要功能：**
    *   **内容来源：** 可使用默认原版闪屏文字、自定义直接输入的文本列表，或本地文件中的文本。
    *   **自定义：** 你可以开启或关闭弹跳效果，并自定义文字颜色、缩放、旋转和阴影。

## 玩家实体
在菜单中渲染玩家模型。

*   **使用场景：**
    *   在主菜单显示当前玩家角色。
    *   创建队伍选择或职业预览界面。
    *   作为“个人资料”区域，显示玩家皮肤和名称。
*   **主要功能：**
    *   **动态外观：** 可复制当前玩家的皮肤、披风和名称。详见 [玩家头颅](./player-heads)。
    *   **自定义姿势：** 可细致控制头部、身体、手臂和腿部的旋转。头部和身体也可设置为跟随鼠标光标。
    *   **属性：** 可设置为幼年、蹲伏，或使用细手臂模型。

## 浏览器
在游戏内渲染实时网页的元素。

此元素需要安装并正常运行 **MCEF（Minecraft Chromium Embedded Framework）** 模组！

你可以从官方项目页面在 [CurseForge](https://www.curseforge.com/minecraft/mc-mods/mcef) 和 [Modrinth](https://modrinth.com/mod/mcef) 下载 MCEF。

对于较新的 Minecraft 版本（1.21.5+），官方 MCEF 项目不提供构建版本，但有一个为最新 Minecraft 版本提供构建版本的分支，可以在 [这里](https://www.curseforge.com/minecraft/mc-mods/mcef-keksuccino)（CurseForge）和 [这里](https://modrinth.com/mod/mcef-keksuccino)（Modrinth）找到。这个分支由 Keksuccino 维护，旨在尽快为最新 Minecraft 版本提供构建版本。

*   **使用场景：**
    *   显示服务器的实时 Dynmap。
    *   嵌入 YouTube 视频播放器。
    *   在游戏内直接显示 wiki 或文档页面。
*   **主要功能：**
    *   **交互性：** 可设置为完全可交互，允许用户点击链接、滚动和输入文字。
    *   **媒体控制：** 提供静音媒体、循环视频和隐藏已加载页面视频控件的选项。

### 加载本地 HTML 文件
浏览器元素可以从 `<game-directory>/config/fancymenu/assets/` 加载本地 HTML 文档。

要加载本地 HTML 文件，请以 `file:///` 开头输入 URL，后面接 SHORT 文件路径，例如 `/config/fancymenu/assets/cool_changelog.html`，这样它看起来就像这样：`file:///config/fancymenu/assets/cool_changelog.html`。

在 **Linux** 上，请使用 [**Absolute File/Folder Path** 占位符](./placeholders#absolute-filefolder-path-absolute_path)，不要硬编码与实例相关的绝对路径：`file:///{"placeholder":"absolute_path","values":{"short_path":"/config/fancymenu/assets/cool_changelog.html"}}`。

Linux 的短路径必须以 `/` 开头，如示例所示。

## 元素动画器
一个用于创建复杂、基于关键帧动画的强大工具。它可以为一个或多个其他元素的位置信息、大小和锚点进行动画化处理。

*   **使用场景：**
    *   让元素滑入或滑出视野。
    *   调整面板或通知的大小。
    *   为位置偏移和锚点过渡制作动画。
*   **主要功能：**
    *   **关键帧编辑器：** 一个专用编辑器，用于在时间轴上添加、编辑和排列关键帧。
    *   **多目标：** 一个动画器可同时控制多个“目标”元素。
    *   **控制：** 动画可设置为循环。你也可以选择只对位置或大小进行动画化。
    *   **时间偏移：** 目标元素可使用各自独立或随机的开始时间偏移。
    *   详见 [元素动画器](./element-animator) 的设置和关键帧编辑说明。

## 计时器
一个不可见元素，会按固定间隔（每次“tick”）执行一系列操作。

> [!NOTE]
> 对于后台自动化，请考虑使用 [调度器](./schedulers)。调度器是全局的，可以独立于特定界面运行。

*   **使用场景：**
    *   定期检查服务器在线状态并更新文本元素。
    *   创建一个更新文本标签的倒计时计时器。
    *   重复运行脚本以创建自定义行为。
*   **主要功能：**
    *   **时间控制：** 可以设置每次 tick 之间的延迟，单位为毫秒。
    *   **Tick 模式：** 可设置为持续 tick、每个游戏会话仅一次，或每次加载菜单时 tick 一次。
    *   **异步：** 可单独运行其 [动作](./action-scripts)，不过启用此选项时某些动作无法运行。

## 音频
一个不可见元素，用于播放音频文件。它可以管理一个音轨播放列表，并提供多种播放控制。

*   **使用场景：**
    *   为菜单添加自定义背景音乐。
    *   创建一个音乐播放器，并通过按钮控制播放（上一曲/下一曲、音量）。
    *   播放环境音景。
*   **主要功能：**
    *   **播放列表：** 可管理多个音轨。
    *   **播放模式：** 可按顺序播放或随机播放曲目（支持曲目权重，使某些曲目出现得更频繁）。
    *   **控制：** 支持循环、音量调整和声音频道选择。详见 [菜单背景音乐](./background-music)。

## 音乐控制器
一个不可见元素，用于在特定菜单中控制 Minecraft 默认音乐播放。

*   **使用场景：**
    *   在你想通过 [**Audio** 元素](#audio) 播放自定义音乐的界面中，禁用默认菜单音乐。
    *   当在游戏内打开菜单时，停止世界中的音乐继续播放。
*   **主要功能：**
    *   可分别切换原版“菜单音乐”和“世界音乐”。

## 进度条
一个可自定义的条形元素，用于直观表示数值。

*   **使用场景：**
    *   使用 `{"placeholder":"world_load_progress"}` 跟踪世界加载进度的加载条。
    *   在游戏内 HUD 中显示生命、饥饿或经验条。
    *   由 [**滑块** 元素](#slider) 控制的音量指示器。
*   **主要功能：**
    *   **动态值：** 进度值（0-100 或 0.0-1.0）通过支持 [占位符](./placeholders) 的文本字段设置。
    *   **外观：** 条的方向（上、下、左、右）、颜色、纹理以及条/背景纹理的九宫切片都可自定义。
    *   **动画：** 带有平滑填充动画，使进度变化看起来不那么突兀。
    *   **基于进度的元素锚点：** 当其他元素将进度条用作其 **元素** 锚点时，启用 **Use Progress for Element Anchor** 可将该锚点移动到当前填充区域的边缘。这样，被锚定的元素会随着条的进度一起移动，而不是始终附着在进度条的静态边界上。

## 拖动器
一个不可见元素，用户可以点击并拖动它来移动位置。其他元素可以锚定到它上面，以创建可移动的小部件。

*   **使用场景：**
    *   创建可拖动的时钟或信息面板。
    *   允许用户按自己的偏好自定义 UI 元素位置。
*   **主要功能：**
    *   **可选持久化：** 启用 **Save User Drag Offset** 可在界面打开和游戏重启后保留用户拖动的位置。禁用后会重置偏移。
    *   **锚点：** 作为其他元素的可移动锚点，是 [定位元素](./positioning-elements) 的关键组成部分。

## 光标
一个不可见元素，当布局处于活动状态时，会用自定义图片替换默认系统光标。

*   **使用场景：**
    *   创建与整合包美学相匹配的完整主题化 UI。
*   **主要功能：**
    *   **自定义纹理：** 光标可使用任意图片。
    *   **热点：** 设置图片中确切作为点击点的像素。详见 [自定义光标](./custom-cursor)。
