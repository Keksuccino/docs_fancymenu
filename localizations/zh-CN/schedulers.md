---
title: 调度器
description: 让 FancyMenu 动作脚本按计时器运行，甚至可以在后台执行。
---

# 调度器

调度器会按计时器运行一个动作脚本。

它们是全局的（不绑定到某个特定界面），因此即使没有打开 GUI，也可以继续运行。

当你需要随着时间自动执行，而不是一次性动作时，就使用调度器。
它们适合重复任务、延迟任务以及后台逻辑。

常见示例：

- 每隔几秒更新变量或文本元素（例如自定义时钟/状态显示）。
- 周期性检查，并在满足条件时触发动作。
- 以定时循环的方式启动菜单效果、音效或其他脚本行为。
- 延迟执行某个动作，并在稍后运行，而不需要界面保持打开。

# 在哪里找到它们

在**布局编辑器**外，打开 FancyMenu 的**菜单栏**，然后进入 **自定义 -> 管理调度器**。

# 快速开始

1. 打开 **自定义 -> 管理调度器**。
2. 点击 **添加调度器**。
3. 配置调度器的 **动作脚本**（这就是每次调度器 tick 时执行的内容）。
4. 选中该调度器并点击 **编辑设置**。
5. 配置：
   - **调度器 ID**（唯一的调度器名称；用于启动/停止动作和条件；允许：`a-z`、`0-9`、`.`、`_`、`-`）
   - **开始延迟 (ms)**（第一次 tick 执行前的等待时间）
   - **Tick 延迟 (ms)**（每次 tick 之间的等待时间；`0` = 每个游戏 tick 都执行）
   - **运行 tick 数**（在自动停止前运行多少个 tick；`0` = 永久运行）
   - **启动时自动运行**（FancyMenu 加载时自动启动此调度器）
6. 使用 **立即启动** 立刻运行它。
7. 使用 **立即停止** 停止它。

# 控制和查看调度器

有一些动作和条件可用于控制调度器，并检查它们是否正在运行。

## 动作

- **启动调度器** 接收调度器的 ID，并在其尚未运行时启动它。
- **停止调度器** 也接收调度器的 ID，并停止它。

## 条件

要检查某个调度器当前是否正在运行，请使用 **调度器正在运行** 条件，并传入该调度器的 ID。

# 提示

1. 使用清晰的 ID，例如 `hud_update`、`menu_animation`、`music_fade`。
2. 先从较高的 tick 延迟开始（例如 `200`-`1000` ms），只有在需要时再降低，以节省性能。
3. 在调度器列表中，右键单击某个调度器可以快速编辑其动作。
4. 在调度器列表中，双击调度器 ID 可以重命名它。
