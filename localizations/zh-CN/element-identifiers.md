---
title: 元素标识符
description: FancyMenu 中元素标识符的工作方式。
---

# 元素标识符

FancyMenu 中的每个元素都有一个唯一标识符，在创建时会自动应用到它上面。

这些标识符的作用，嗯，就是用来识别该元素，这样 FancyMenu 的其他部分，比如占位符、操作和加载要求，就可以与该元素进行通信。

# 获取元素的标识符

如果你需要在别处使用某个元素的标识符，只需在编辑器中**右键单击**该元素，然后点击 **Copy Element Identifier**。这样会将该元素的标识符复制到剪贴板，方便你在需要的地方直接粘贴。
