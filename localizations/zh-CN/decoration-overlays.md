---
title: 装饰叠加层
description: 在 FancyMenu 布局编辑器中为菜单添加全屏视觉叠加效果。
---
# 装饰叠加层

装饰叠加层是一种全屏效果，会渲染在你的菜单元素前方。

当你想为菜单添加氛围感或动态效果，而不想手动构建这些效果时，它们非常有用。

# 在哪里找到它

在布局编辑器中打开一个布局，然后右键点击编辑器背景，打开 **装饰叠加层**。

# 快速开始

1. 在布局编辑器中打开一个布局。
2. 右键点击背景（空白区域）。
3. 打开 **装饰叠加层**。
4. 选择一种叠加层类型。
5. 将 **显示叠加层** 设置为 **启用**。
6. 配置叠加层设置。
7. 保存布局并测试该界面。

# 叠加层类型的工作方式

每种叠加层类型都有各自的子菜单和独立的 **显示叠加层** 开关。

- 你可以只启用需要的类型。
- 你可以在同一个布局中组合多个已启用的类型。
- 每种叠加层类型都有各自的设置（例如颜色、强度、速度、密度、缩放和特殊行为）。

> [!INFO]
> 你可以通过使用多个启用了相同类型的布局，叠加同一种叠加层类型的多个实例。

# 叠加层类型

- **降雪**：带有可选的表面/按钮积雪效果的降雪。
- **降雨**：带有可选水坑、滴水以及可选雷闪效果的下雨。
- **萤火虫**：可移动的萤火虫群组，支持配置群组数量、密度、大小和颜色。
- **串灯**：可配置的串灯组合、灯光颜色、风吹/闪烁行为以及节日配色模式。
- **落叶**：带有可配置颜色、风、速度、缩放和密度的飘落树叶。
- **烟花**：高频烟花，支持配置数量、爆炸大小和缩放。
- **彩纸**：彩纸雨，支持可选的鼠标点击彩纸模式。
- **Buddy**：一个可互动的虚拟宠物，拥有饥饿、快乐、精力、娱乐、活动、升级、成就以及持久化状态。
- **浏览器**：带有 URL 和媒体设置的全屏浏览器叠加层。
- **GLSL Shader**：全屏自定义着色器叠加层（用于基于着色器的动画或静态视觉效果）。

# Buddy 虚拟宠物

**Buddy** 叠加层不只是一个视觉角色，而是一个 Tamagotchi 风格的虚拟宠物。它会沿着屏幕底部行走，显示代表需求的思考气泡，对交互作出反应，并在游戏会话之间保留状态。

## 需求与控制

Buddy 会跟踪四个从 `0` 到 `100` 的数值：

- **饥饿** 会随时间下降，可通过喂食恢复。
- **快乐** 会随时间下降，并可通过照顾提升，包括抚摸和玩耍。
- **精力** 在清醒时和进行活动时会下降，睡觉时会恢复。
- **娱乐** 会随时间下降，并在玩耍时提升。

使用这些鼠标控制和状态界面来照顾它：

- **左键点击 Buddy** 可抚摸它。它睡觉时左键点击会把它叫醒，并造成轻微的快乐惩罚。
- **右键点击 Buddy** 可打开其状态界面。统计标签页会显示四项需求、等级和 XP；成就标签页会显示成就进度。
- 在状态界面中选择 **喂食**，然后将食物拖到 Buddy 上。食物会恢复饥饿和快乐。
- 在状态界面中选择 **玩耍**，然后拖动并释放球。球会根据鼠标移动计算投掷速度，Buddy 可以追逐、接住、拿住并与球玩耍。
- 当按钮可用时，选择 **睡觉** 可恢复精力。Buddy 也会在精力低到危险程度时自动入睡。
- Buddy 偶尔会留下便便。**左键点击便便** 可清理它。如果屏幕上一直保留至少三个便便，快乐值会持续下降，直到少于三个为止；最大便便数量可配置。

## XP、等级与成就

照顾 Buddy、清理便便、维持良好需求状态以及完成其他里程碑都会奖励 XP。Buddy 从 1 级开始，最高可达到 30 级。更高等级会逐步降低饥饿、快乐和精力的衰减速度（在 30 级时最高可降低 50%），并改善多种照顾与 XP 效果。

成就会追踪交互、属性、等级、会话以及特殊里程碑。右键打开状态界面即可查看这两个进度系统。

## 死亡与重置存档

默认启用 **Buddy Can Die**。如果饥饿或快乐中任意一项持续为 `0` 达到 **10 个现实小时**，Buddy 就会死亡并被一块墓碑取代。在计时器到期前将该数值恢复到非零，会重置该需求对应的计时器；禁用 **Buddy Can Die** 会清除两个计时器。

在 Buddy 死亡后，左键点击墓碑即可重新开始。你也可以随时在 Buddy 叠加层设置中使用 **重置 Buddy 存档**。重置会移除该叠加层实例的宠物状态存档，以及单独的升级/成就存档。

> [!WARNING]
> 重置 Buddy 存档会永久删除其需求、等级、XP、成就、活动计数器以及保存的便便状态。

## 持久化与自定义

Buddy 的状态会大约每两分钟自动保存一次，并在其界面关闭时保存。宠物状态和等级状态会为每个叠加层实例分别使用 `<game-directory>/fancymenu_data/buddy/` 中的独立 JSON 文件。完整的 FancyMenu 路径参考请参见 [数据存储位置](./data-storage-locations)。

叠加层设置还允许你替换 Buddy 的精灵图集、交互物品、需求图标、状态界面纹理以及墓碑。高级属性设置可控制衰减、活动消耗与收益、照顾效果、最大便便数量，以及是否启用死亡。

# 浏览器叠加层：交互式与被动式

浏览器叠加层可以配置为交互式浏览器或被动视觉层。

- **处理鼠标/键盘** 设置控制浏览器自身是否处理输入。
- **消耗鼠标/键盘** 设置控制输入是否会被其后方的菜单阻止。

实际配置示例：

- 前景交互式浏览器：同时启用 **处理** 和 **消耗**。
- 仅视觉显示的浏览器图层：禁用 **处理**，并禁用 **消耗**。

> [!IMPORTANT]
> 浏览器装饰叠加层需要 **MCEF** 模组。
