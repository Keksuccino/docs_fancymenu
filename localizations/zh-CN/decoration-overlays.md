---
title: 装饰叠加层
description: 在 FancyMenu 布局编辑器中为菜单添加全屏视觉叠加效果。
---
# 装饰叠加层

装饰叠加层是在你的菜单元素前方渲染的全屏效果。

当你想为菜单添加氛围或动效，而不想手动构建这些效果时，它们非常有用。

# 在哪里找到它

在布局编辑器中打开一个布局，然后右键点击编辑器背景并打开 **装饰叠加层**。

# 快速开始

1. 在布局编辑器中打开一个布局。
2. 右键单击背景（空白区域）。
3. 打开 **装饰叠加层**。
4. 选择一种叠加层类型。
5. 将 **显示叠加层** 设置为 **启用**。
6. 配置叠加层设置。
7. 保存布局并测试界面。

# 叠加层类型的工作方式

每种叠加层类型都有自己的子菜单和各自的 **显示叠加层** 开关。

- 你可以只启用你想要的类型。
- 你可以在同一个布局中组合多个已启用的类型。
- 设置按叠加层类型分别配置（例如颜色、强度、速度、密度、缩放以及特殊行为）。

> [!INFO]
> 可以通过使用多个启用了相同类型的布局，来叠加同一叠加层类型的多个实例。

# 叠加层类型

- **降雪**：下雪，并可选在表面/按钮上积雪。
- **降雨**：下雨，并可选出现水洼、滴水以及雷电闪光。
- **萤火虫**：移动的萤火虫群，可配置群组数量、密度、大小和颜色。
- **串灯**：可配置的灯串组合、灯光颜色、风/闪烁行为以及节日配色模式。
- **落叶**：飘落的树叶，可配置颜色、风、速度、缩放和密度。
- **烟花**：频繁出现的烟花，可配置数量、爆炸大小和缩放。
- **彩纸屑**：彩纸屑雨，并可选鼠标点击彩纸屑模式。
- **Buddy**：一个互动式虚拟宠物，拥有饥饿、快乐、精力、娱乐、活动、升级、成就以及持久化状态。
- **浏览器**：带 URL 和媒体设置的全屏浏览器叠加层。
- **GLSL 着色器**：全屏自定义着色器叠加层（用于基于着色器的动画或静态视觉效果）。

# Buddy 虚拟宠物

**Buddy** 叠加层不仅仅是一个视觉角色，它是一个类似电子宠物（Tamagotchi）的虚拟宠物。它会沿着屏幕底部移动，为自己的需求显示思考气泡，对互动作出反应，并且会在游戏会话之间保留状态。

## 需求与控制

Buddy 会跟踪四项数值，从 `0` 到 `100`：

- **饥饿** 会随时间下降，并可通过食物恢复。
- **快乐** 会随时间下降，并可通过照顾提升，包括抚摸和玩耍。
- **精力** 在清醒和活动期间下降，然后在睡眠时恢复。
- **娱乐** 会随时间下降，并在玩耍时提升。

使用这些鼠标控制和状态界面来照顾它：

- **左键点击 Buddy** 可以抚摸它。Buddy 睡觉时左键点击会唤醒它，并施加少量快乐惩罚。
- **右键点击 Buddy** 可打开其状态界面。统计信息标签页会显示四项需求、等级和 XP；成就标签页会显示成就进度。
- 在状态界面中选择 **喂食**，然后把食物拖到 Buddy 身上。食物会恢复饥饿和快乐。
- 在状态界面中选择 **玩耍**，然后拖动并松开球。球会使用鼠标移动来计算投掷速度，Buddy 可以追逐、接住、拿住并和它玩耍。
- 当按钮可用时选择 **睡觉** 以恢复精力。当精力低到危险程度时，Buddy 也会自动睡着。
- Buddy 偶尔会留下便便。**左键点击便便** 可清理它。如果屏幕上持续存在至少三个便便，快乐会不断降低，直到少于三个；最大便便数量可配置。

## XP、等级与成就

照顾 Buddy、清理便便、维持良好的需求状态，以及完成其他里程碑，都会奖励 XP。Buddy 初始为 1 级，最高可达到 30 级。更高等级会逐步降低饥饿、快乐和精力的消耗速度（在 30 级时最多减少 50%），并提升多种照顾和 XP 效果。

成就会跟踪互动、属性、等级、会话以及特殊里程碑。右键打开状态界面即可查看这两套进度系统。

## 死亡与重置存档

默认启用 **Buddy 会死亡**。如果饥饿或快乐中的任一项持续为 `0` 达 **10 个真实小时**，Buddy 就会死亡并被墓碑取代。在计时器到期前把该数值从 0 提升回来，会重置对应需求的计时器；禁用 **Buddy 会死亡** 会清除两个计时器。

死亡后要重新开始，只需左键单击墓碑。你也可以随时在 Buddy 叠加层设置中使用 **重置 Buddy 存档**。重置会移除此叠加层实例的宠物状态存档以及独立的等级/成就存档。

> [!WARNING]
> 重置 Buddy 存档会永久移除它的需求、等级、XP、成就、活动计数器以及保存的便便状态。

## 持久化与自定义

Buddy 的状态会大约每两分钟自动保存一次，并在其界面关闭时保存。宠物状态和等级状态在 `<game-directory>/fancymenu_data/buddy/` 中为每个叠加层实例分别使用独立的 JSON 文件。完整的 FancyMenu 路径参考请参见 [数据存储位置](./data-storage-locations)。

叠加层设置还允许你替换 Buddy 的精灵图集、互动物品、需求图标、状态界面纹理以及墓碑。高级属性设置可控制衰减、活动消耗与收益、照顾效果、最大便便数量，以及是否启用死亡。

# 浏览器叠加层：交互式 vs 被动式

浏览器叠加层可以配置为交互式浏览器，或者被动的视觉层。

- **处理鼠标/键盘** 设置控制浏览器本身是否处理输入。
- **消耗鼠标/键盘** 设置控制输入是否会阻止其后方的菜单接收输入。

实际配置示例：

- 前置交互式浏览器：同时启用 **处理** 和 **消耗**。
- 仅作为视觉层的浏览器：禁用 **处理** 并禁用 **消耗**。

> [!IMPORTANT]
> 浏览器装饰叠加层需要 **[Rinku](https://modrinth.com/mod/rinku)** 模组。
