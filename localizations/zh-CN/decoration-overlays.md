---
title: 装饰覆盖层
description: 在 FancyMenu 布局编辑器中为菜单添加全屏视觉覆盖层。
---

# 装饰覆盖层

装饰覆盖层是会渲染在菜单元素前方的全屏特效。

当你想为菜单添加氛围或动效，但又不想手动构建这些效果时，它们非常有用。

常见示例：

- 添加 **降雪**，为冬季菜单营造积雪效果。
- 添加 **降雨**，打造带有水洼和雨滴的暴风雨氛围。
- 添加 **萤火虫**，为安静的夜晚风格菜单增添点缀。
- 添加 **串灯**，用于节日或装饰性菜单主题。
- 添加 **落叶**、**烟花** 或 **彩纸**，用于季节/活动菜单。
- 添加 **浏览器** 覆盖层，以显示全屏网页/视频层。

# 在哪里找到它

在布局编辑器中打开一个布局，然后右键编辑器背景，打开 **装饰覆盖层**。

# 快速开始

1. 在布局编辑器中打开一个布局。
2. 右键背景（空白区域）。
3. 打开 **装饰覆盖层**。
4. 选择一种覆盖层类型。
5. 将 **显示覆盖层** 设置为 **启用**。
6. 配置覆盖层设置。
7. 保存布局并测试界面。

# 覆盖层类型的工作方式

每种覆盖层类型都有自己的子菜单，以及各自的 **显示覆盖层** 开关。

- 你可以只启用需要的类型。
- 你可以在同一个布局中组合多个已启用的类型。
- 设置是按覆盖层类型分别保存的（例如颜色、强度、速度、密度、缩放、特殊行为）。

> [!INFO]
> 可以通过使用多个启用同一类型的布局，叠加同一种覆盖层类型的多个实例。

# 覆盖层类型

- **降雪**：带有可选雪层堆积效果的降雪。
- **降雨**：带有可选水洼、雨滴和雷电闪光的降雨。
- **萤火虫**：可移动的萤火虫群组，可配置群组数量、密度、大小和颜色。
- **串灯**：可配置的串灯组合、灯光颜色、风/闪烁行为以及节日配色模式。
- **落叶**：会飘落的树叶，可配置颜色、风、速度、缩放和密度。
- **烟花**：频繁出现的烟花，可配置数量、爆炸大小和缩放。
- **彩纸**：彩纸雨，可选鼠标点击彩纸模式。
- **浏览器**：带有 URL 和媒体设置的全屏浏览器覆盖层。
- **GLSL 着色器**：全屏自定义着色器覆盖层（用于基于着色器的动画或静态视觉效果）。

# 浏览器覆盖层：交互式与被动式

浏览器覆盖层可以配置为交互式浏览器，也可以配置为被动的视觉层。

- **处理鼠标/键盘** 设置决定浏览器本身是否处理输入。
- **消耗鼠标/键盘** 设置决定输入是否会被其后方的菜单阻挡。

实际配置示例：

- 前方的交互式浏览器：同时启用 **处理** 和 **消耗**。
- 仅用于视觉展示的浏览器层：禁用 **处理**，并禁用 **消耗**。

> [!IMPORTANT]
> 浏览器装饰覆盖层需要 **MCEF** 模组。
