---
title: Слайд-шоу
description: Створюйте та використовуйте слайд-шоу із зображень.
---

# Слайд-шоу

Кожне слайд-шоу має власний каталог у:

```text
<game-directory>/config/fancymenu/slideshows/
```

`<game-directory>` — це активний екземпляр лаунчера, який може відрізнятися від стандартного каталогу `.minecraft`.

# Структура каталогів

```text
<game-directory>/config/fancymenu/slideshows/
└── myslideshow/
    ├── properties.txt
    ├── overlay.png          # необов’язково
    └── images/
        ├── image_01.png
        └── image_02.jpg
```

Зображення мають використовувати розширення `.png` або `.jpg`; інші розширення, зокрема `.jpeg`, ігноруються.

Якщо `randomize = false`, зображення відтворюються в алфавітному порядку назв файлів без урахування регістру. Використовуйте назви з нулями на початку, наприклад `image_01.png`, `image_02.png` і `image_10.png`.

# `properties.txt`

```text
type = slideshow

slideshow-meta {
  name = cool_slideshow
  width = 1920
  height = 1080
  x = 0
  y = 0
  duration = 5.0
  fadespeed = 12.0
  randomize = false
}
```

| Властивість | Значення |
|---|---|
| `name` | Обов’язковий ідентифікатор під час виконання, чутливий до регістру; він має бути унікальним |
| `width`, `height` | Базовий розмір у пікселях із масштабуванням GUI та вихідне співвідношення сторін |
| `x`, `y` | Базова позиція верхнього лівого кута; звичайні елементи та фони використовують власну позицію, тому залишайте ці значення `0` |
| `duration` | Мінімальна кількість секунд між початками переходів; включає час згасання та має бути більшою за `0` |
| `fadespeed` | Множник швидкості згасання; `1.0` — стандартне значення, більші значення пришвидшують згасання, а значення має бути більшим за `0` |
| `randomize` | `true` для випадкового вибору або `false` для порядку назв файлів |

Обов’язковою є лише властивість `name`. Значення за замовчуванням: `width = 50`, `height = 50`, `x = 0`, `y = 0`, `duration = 10.0`, `fadespeed = 1.0` і `randomize = false`. Не змінюйте `type = slideshow` і `slideshow-meta`; записуйте по одному `key = value` у кожному рядку та використовуйте крапку для десяткових чисел.

У розкладках вибирається значення `name`, а не назва каталогу. Дублікати назв не відхиляються, а порядок сканування каталогів визначає, яке слайд-шоу залишиться доступним. Назви в каталозі слайд-шоу мають бути унікальними.

У випадковому режимі під час кожного переходу незалежно вибирається зображення; якщо доступно кілька зображень, негайний повтор не допускається. Час вимірюється в реальному часі: якщо згасання триває довше за `duration`, наступний перехід затримується, а після повернення до прихованого слайд-шоу воно може одразу перейти до наступного зображення.

# Використання слайд-шоу

Перезавантажте FancyMenu через **Customization -> Reload FancyMenu** або перезапустіть клієнт. Використовуйте елемент [**Slideshow**](./elements#slideshow) або клацніть правою кнопкою миші по фону редактора розкладки й виберіть [**Menu Backgrounds**](./menu-backgrounds) -> **Slideshow**.
