---
title: Условия (требования)
description: Как использовать требования загрузки.
---
# Требования

Требования (в некоторых меню называются **Loading Requirements**) показывают или скрывают контент в зависимости от условий, таких как состояние наведения, размер окна или загружен ли мир.

Их можно использовать на [элементах](./elements), для целых макетов и в [сценариях действий](./action-scripts).

# Добавление требований к элементам

Чтобы добавить требования к элементу, щёлкните его правой кнопкой мыши и выберите **Loading Requirements**.

Требования проверяются, пока меню открыто, поэтому элементы обновляются при изменении условия.

# Требования для всего макета

Вы также можете менять видимость целых макетов, щёлкнув правой кнопкой мыши по **фону редактора**, а затем выбрав **Loading Requirements [Layout-Wide]**.

Когда результат требования для всего макета меняется, FancyMenu перестраивает текущий экран и применяет макеты, чьи требования теперь выполняются.

# Сценарии действий

Требования также можно использовать в сценариях действий.
Их можно добавить в редакторе сценариев действий и использовать для выполнения определённых действий только если условие требования выполнено.

# Объединение требований

- Требования вне групп используют **AND**, поэтому все они должны выполняться.
- Внутри группы выберите **AND** или **OR**.
- Используйте **IF NOT**, чтобы инвертировать одно требование.

Эти правила одинаковы для элементов, макетов и сценариев действий.

# Значения требований

Для требований, которым нужно значение, используйте **Edit Requirement Value** и следуйте описанию, показанному в редакторе. Некоторые поля поддерживают автодополнение по **TAB**.

Если импортированное требование перестало работать после обновления FancyMenu или аддонов, отредактируйте его в окне требований и проверьте `logs/latest.log` на наличие ошибок.

Редактор требований поддерживает контекстное меню по правой кнопке мыши, навигацию с клавиатуры, поиск, отмену/повтор (`Ctrl/Command + Z` / `Ctrl/Command + Y`) и сохранение через `Ctrl/Command + S`.

# Требования подробнее

В этом разделе перечислены встроенные требования FancyMenu.

## Наведение на элемент (`fancymenu_visibility_requirement_is_element_hovered`)

**Назначение:** Проверяет, находится ли указанный элемент под курсором мыши.

**Значение:** Обязательно — [Идентификатор элемента](./element-identifiers) целевого элемента (например, `some_element_ID`).

## Элемент в фокусе (`is_element_focused`)

**Назначение:** Проверяет, находится ли указанный элемент в текущий момент в фокусе клавиатуры (например, текстовое поле или кнопка в фокусе).

**Значение:** Обязательно — ID элемента цели (тот же ID, что показан в редакторе)

> [!NOTE]
> Фокус и наведение — это разные состояния. Элемент может сохранять вид в состоянии фокуса после того, как указатель уходит с него; клик или навигация с клавиатуры могут перевести его в фокус.

## Наведён ли любой элемент (`fancymenu_visibility_requirement_is_any_element_hovered`)

**Назначение:** Проверяет видимые/рендерящиеся элементы в текущем активном слое кастомизации, включая элементы, добавленные наложенными макетами.

**Значение:** Не требуется

## Наведена ли любая кнопка (`fancymenu_visibility_requirement_is_any_button_hovered`)

**Назначение:** Проверяет, наведена ли любая видимая/рендерящаяся ванильная или пользовательская кнопка в текущем активном слое кастомизации, включая кнопки, добавленные наложенными макетами.

**Значение:** Не требуется

## Макет включён (`fancymenu_visibility_requirement_is_layout_enabled`)

**Назначение:** Проверяет, включён ли в данный момент определённый макет.

**Значение:** Обязательно — имя макета (например, `my_cool_main_menu_layout`)

## Запущен планировщик (`fancymenu_visibility_requirement_is_scheduler_running`)

**Назначение:** Проверяет, запущен ли в данный момент [планировщик](./schedulers).

**Значение:** Обязательно — ID планировщика (например, `my_scheduler`)

## Масштаб GUI (`fancymenu_loading_requirement_is_gui_scale`)

**Назначение:** Проверяет, соответствует ли текущий масштаб GUI определённым условиям.

**Значение:** Обязательно — используйте число для равенства, `>` для больше, или `<` для меньше.

Несколько условий, разделённых запятыми, объединяются через AND. Например, `>1,<4` выполняется только тогда, когда масштаб GUI больше `1` и меньше `4`.

## Кнопка активна (`fancymenu_visibility_requirement_is_button_active`)

**Назначение:** Проверяет, активна ли (кликабельна) определённая кнопка.

**Значение:** Обязательно — ID элемента целевой кнопки (например, "some_element_ID")

## Заголовок экрана (`is_menu_title`)

**Назначение:** Проверяет, совпадает ли отображаемый заголовок экрана с определённым текстом или ключом локализации. Проверяется только отображаемое имя/заголовок экрана, например "Options" или "Pause". Идентификатор меню/экрана (например, `title_screen`) НЕ проверяется!

**Значение:** Обязательно — точный текст заголовка или ключ локализации экрана

## Нажата клавиша (`is_key_pressed`)

**Назначение:** Проверяет, нажата ли в данный момент определённая клавиша клавиатуры.

**Значение:** Обязательно — код клавиши цели. Выбирается через интерфейс при редактировании значения требования.

## Открыт ли какой-либо экран (`is_any_screen_open`)

**Назначение:** Проверяет, открыт ли в данный момент какой-либо экран/меню (возвращает false, если экран не отображается).

**Значение:** Не требуется

## Включено ли отладочное наложение MC (`is_debug_overlay_enabled`)

**Назначение:** Проверяет, видно ли в данный момент отладочное наложение F3.

**Значение:** Не требуется

## Активный тип курсора (`is_active_cursor_type`)

**Назначение:** Проверяет, совпадает ли текущий активный тип курсора FancyMenu с определённым стандартным типом курсора.

**Значение:** Обязательно — тип курсора: `normal`, `writing`, `crosshair`, `pointing_hand`, `resize_horizontal`, `resize_vertical`, `resize_nwse`, `resize_nesw`, `resize_all` или `not_allowed`

## Видна ли панель меню кастомизации (`is_customization_menu_bar_visible`)

**Назначение:** Проверяет, видна ли в данный момент панель меню кастомизации FancyMenu.

**Значение:** Не требуется

## Включён ли Modpack Mode (`is_modpack_mode_enabled`)

**Назначение:** Проверяет, включён ли Modpack Mode FancyMenu.

**Значение:** Не требуется

## Нажата кнопка мыши (`mouse_click`)

**Назначение:** Возвращает true, пока удерживается определённая кнопка мыши. Это не одноразовое событие клика; используйте [**On Mouse Button Clicked** listener](./listeners#on-mouse-button-clicked-mouse_button_clicked), если действие должно выполняться один раз на каждый клик.

**Значение:** Обязательно — `left` или `right`, чтобы указать, какую кнопку мыши проверять

## Полноэкранный режим (`fancymenu_loading_requirement_is_fullscreen`)

**Назначение:** Проверяет, запущена ли игра в полноэкранном режиме.

**Значение:** Не требуется

## Ширина окна (`fancymenu_loading_requirement_is_window_width`)

**Назначение:** Проверяет, соответствует ли ширина окна игры определённым значениям.

**Значение:** Обязательно — ширина окна в пикселях (например, "1920"). Можно указать несколько значений, разделив их запятыми.

## Высота окна (`fancymenu_loading_requirement_is_window_height`)

**Назначение:** Проверяет, соответствует ли высота окна игры определённым значениям.

**Значение:** Обязательно — высота окна в пикселях (например, "1080"). Можно указать несколько значений, разделив их запятыми.

## Ширина окна больше чем (`fancymenu_loading_requirement_is_window_width_bigger_than`)

**Назначение:** Проверяет, больше ли ширина окна игры определённого значения.

**Значение:** Обязательно — ширина окна в пикселях (например, "1920")

## Высота окна больше чем (`fancymenu_loading_requirement_is_window_height_bigger_than`)

**Назначение:** Проверяет, больше ли высота окна игры определённого значения.

**Значение:** Обязательно — высота окна в пикселях (например, "1080")

## Многопользовательская игра (`fancymenu_loading_requirement_is_multiplayer`)

**Назначение:** Проверяет, находится ли игрок в многопользовательском мире.

**Значение:** Не требуется

## Одиночная игра (`fancymenu_loading_requirement_is_singpleplayer`)

**Назначение:** Проверяет, находится ли игрок в мире одиночной игры.

**Значение:** Не требуется

## Мир загружен (`fancymenu_loading_requirement_is_world_loaded`)

**Назначение:** Проверяет, загружен ли в данный момент какой-либо мир.

**Значение:** Не требуется

## Режим приключения (`fancymenu_visibility_requirement_is_adventure`)

**Назначение:** Проверяет, находится ли игрок в режиме приключения.

**Значение:** Не требуется

## Творческий режим (`fancymenu_visibility_requirement_is_creative`)

**Назначение:** Проверяет, находится ли игрок в творческом режиме.

**Значение:** Не требуется

## Режим наблюдателя (`fancymenu_visibility_requirement_is_spectator`)

**Назначение:** Проверяет, находится ли игрок в режиме наблюдателя.

**Значение:** Не требуется

## Режим выживания (`fancymenu_visibility_requirement_is_survival`)

**Назначение:** Проверяет, находится ли игрок в режиме выживания.

**Значение:** Не требуется

## Режим игры (`is_gamemode`)

**Назначение:** Проверяет, находится ли игрок в определённом игровом режиме.

**Значение:** Обязательно — имя игрового режима (например, "creative", "survival", "adventure", "spectator")

## Сложность (`is_difficulty`)

**Назначение:** Проверяет, соответствует ли текущая сложность игры определённому значению.

**Значение:** Обязательно — название сложности (например, "peaceful", "easy", "normal", "hard")

## Хардкор (`is_hardcore`)

**Назначение:** Проверяет, находится ли загруженный мир в режиме хардкора.

**Значение:** Не требуется

## Ракурс камеры (`is_camera_perspective`)

**Назначение:** Проверяет, соответствует ли текущий ракурс камеры определённому ракурсу.

**Значение:** Обязательно — `first_person`, `third_person_back` или `third_person_front`

## Идёт дождь (`is_raining`)

**Назначение:** Проверяет, идёт ли сейчас дождь в местоположении игрока.

**Значение:** Не требуется

## Гремит гром (`is_thundering`)

**Назначение:** Проверяет, идёт ли сейчас гроза в мире игрока.

**Значение:** Не требуется

## Ясная погода (`is_clear_weather`)

**Назначение:** Проверяет, ясная ли сейчас погода (нет дождя и грома).

**Значение:** Не требуется

## Идёт снег (`is_snowing`)

**Назначение:** Проверяет, идёт ли сейчас снег в местоположении игрока.

**Значение:** Не требуется

## Игрок бежит (`is_player_running`)

**Назначение:** Проверяет, бежит ли игрок в спринте.

**Значение:** Не требуется

## Игрок крадётся (`is_player_sneaking`)

**Назначение:** Проверяет, крадётся ли игрок.

**Значение:** Не требуется

## Игрок использует предмет (`is_player_using_item`)

**Назначение:** Проверяет, использует ли игрок предмет в данный момент.

**Значение:** Не требуется

## Игрок плывёт (`is_player_swimming`)

**Назначение:** Проверяет, плывёт ли игрок.

**Значение:** Не требуется

## Игрок прыгает или падает (`is_player_jumping`)

**Назначение:** Возвращает true, пока игрок находится в воздухе в обычном состоянии прыжка или падения. Плавание, жидкости, полёт на элитрах, сон, визуальное плавание и ползание исключаются.

**Значение:** Не требуется

## Игрок под водой (`is_player_under_water`)

**Назначение:** Проверяет, находится ли игрок полностью под водой.

**Значение:** Не требуется

## Игрок в воде (`is_player_in_water`)

**Назначение:** Проверяет, находится ли игрок в воде (может быть частично погружён).

**Значение:** Не требуется

## Игрок в лаве (`is_player_in_lava`)

**Назначение:** Проверяет, находится ли игрок в лаве.

**Значение:** Не требуется

## Игрок в жидкости (`is_player_in_fluid`)

**Назначение:** Проверяет, находится ли игрок в любой жидкости (вода, лава и т. д.).

**Значение:** Не требуется

## Игрок едет на сущности/транспорте (`is_player_riding_entity`)

**Назначение:** Проверяет, едет ли игрок на какой-либо сущности.

**Значение:** Не требуется

## Игрок едет на прыгаемой сущности (`is_player_riding_jumpable_entity`)

**Назначение:** Проверяет, едет ли игрок на сущности, которая умеет прыгать (например, на лошади).

**Значение:** Не требуется

## Игрок едет на сущности с здоровьем (`is_player_riding_entity_with_health`)

**Назначение:** Проверяет, едет ли игрок на живой сущности с здоровьем (например, на животных, но не на лодках).

**Значение:** Не требуется

## Игрок в порошковом снегу (`is_player_in_powder_snow`)

**Назначение:** Проверяет, находится ли игрок в порошковом снегу.

**Значение:** Не требуется

## Игрок был в порошковом снегу (`was_player_in_powder_snow`)

**Назначение:** Проверяет, был ли игрок в порошковом снегу (используется для эффектов, которые сохраняются после выхода).

**Значение:** Не требуется

## Игрок носит тыкву (`is_player_wearing_pumpkin`)

**Назначение:** Проверяет, надета ли на голове игрока резная тыква.

**Значение:** Не требуется

## Игрок летит на элитрах (`is_player_flying_with_elytra`)

**Назначение:** Проверяет, летит ли игрок на элитрах.

**Значение:** Не требуется

## Игрок летит в творческом режиме (`is_player_creative_flying`)

**Назначение:** Проверяет, летит ли игрок в творческом режиме.

**Значение:** Не требуется

## Есть сердца поглощения (`has_player_absorption_hearts`)

**Назначение:** Проверяет, есть ли у игрока сердца поглощения (золотые сердца).

**Значение:** Не требуется

## Игрок под эффектом иссушения (`is_player_withered`)

**Назначение:** Проверяет, находится ли игрок под эффектом иссушения.

**Значение:** Не требуется

## Игрок полностью заморожен (`is_player_fully_frozen`)

**Назначение:** Проверяет, полностью ли игрок заморожен (обычно из-за порошкового снега).

**Значение:** Не требуется

## Игрок отравлен (`is_player_poisoned`)

**Назначение:** Проверяет, находится ли игрок под эффектом отравления.

**Значение:** Не требуется

## Игрок в биоме (`is_player_in_biome`)

**Назначение:** Проверяет, находится ли игрок в определённом биоме.

**Значение:** Обязательно — идентификатор биома (например, `minecraft:birch_forest`)

## Игрок в измерении (`is_player_in_dimension`)

**Назначение:** Проверяет, находится ли игрок в определённом измерении.

**Значение:** Обязательно — идентификатор измерения (например, `minecraft:overworld`, `minecraft:the_nether`, `minecraft:the_end`)

## Игрок в структуре (`is_player_in_structure`)

**Назначение:** Проверяет, находится ли игрок в данный момент внутри определённой структуры. Для серверных миров требуется FancyMenu на сервере.

**Значение:** Обязательно — идентификатор структуры (например, `minecraft:village`)

## Рядом есть сущность (`is_entity_nearby`)

**Назначение:** Проверяет, находится ли определённый тип сущности в пределах заданного радиуса от игрока.

**Значение:** Обязательно — формат: "радиус:entity_id" (например, `10:minecraft:pig` — проверяет наличие свиней в пределах 10 блоков)

## Эффект активен (`is_effect_active`)

**Назначение:** Проверяет, активен ли у игрока определённый эффект зелья.

**Значение:** Обязательно — идентификатор эффекта (например, `minecraft:speed`, `minecraft:strength`)

## Любой эффект активен (`is_any_effect_active`)

**Назначение:** Проверяет, есть ли у игрока какой-либо активный эффект зелья.

**Значение:** Не требуется

## Игрок левша (`is_left_handed`)

**Назначение:** Проверяет, включён ли у игрока режим левши в настройках игры.

**Значение:** Не требуется

## Слот инвентаря заполнен (`is_inventory_slot_filled`)

**Назначение:** Проверяет, содержит ли определённый слот инвентаря предмет.

**Значение:** Обязательно — номер слота (0-35 для основного инвентаря, слоты 0-8 — это хотбар)

## Предмет наведен в инвентаре (`is_item_hovered_in_inventory`)

**Назначение:** Проверяет, наведён ли курсор на какой-либо предмет в окне инвентаря.

**Значение:** Не требуется

## Курсор держит предмет инвентаря (`is_cursor_holding_inventory_item`)

**Назначение:** Проверяет, удерживает ли курсор в данный момент стек предметов из инвентаря.

**Значение:** Не требуется

## Выбран слот хотбара (`is_hotbar_slot_active`)

**Назначение:** Проверяет, выбран ли в данный момент определённый слот хотбара.

**Значение:** Обязательно — номер слота хотбара (0-8)

## У игрока есть уровень прав (`fancymenu_loading_requirement_has_player_permission_level`)

**Назначение:** Проверяет, имеет ли игрок как минимум указанный уровень прав/OP в текущем мире или на сервере.

**Значение:** Обязательно — число уровня прав (0-4, где 4 — оператор сервера)

## Ослаблена сила атаки (`is_attack_strength_weakened`)

**Назначение:** Проверяет, ослаблена ли сила атаки игрока в данный момент (не полностью заряжена).

**Значение:** Не требуется

## Реальное время: день (`fancymenu_visibility_requirement_is_realtime_day`)

**Назначение:** Проверяет, совпадает ли текущий календарный день месяца с определённым значением.

**Значение:** Обязательно — номер дня (1-31). Можно указать несколько значений, разделив их запятыми.

## Реальное время: час (`fancymenu_visibility_requirement_is_realtime_hour`)

**Назначение:** Проверяет, совпадает ли текущий час по реальному времени с определённым значением.

**Значение:** Обязательно — час в 24-часовом формате (0-23). Можно указать несколько значений, разделив их запятыми.

## Реальное время: минута (`fancymenu_visibility_requirement_is_realtime_minute`)

**Назначение:** Проверяет, совпадает ли текущая минута по реальному времени с определённым значением.

**Значение:** Обязательно — минута (0-59). Можно указать несколько значений, разделив их запятыми.

## Реальное время: месяц (`fancymenu_visibility_requirement_is_realtime_month`)

**Назначение:** Проверяет, совпадает ли текущий месяц по реальному времени с определённым значением.

**Значение:** Обязательно — номер месяца (1-12, где 1 — январь). Можно указать несколько значений, разделив их запятыми.

## Реальное время: секунда (`fancymenu_visibility_requirement_is_realtime_second`)

**Назначение:** Проверяет, совпадает ли текущая секунда по реальному времени с определённым значением.

**Значение:** Обязательно — секунда (0-59). Можно указать несколько значений, разделив их запятыми.

## Реальное время: день недели (`fancymenu_visibility_requirement_is_realtime_week_day`)

**Назначение:** Проверяет, совпадает ли текущий день недели по реальному времени с определённым значением.

**Значение:** Обязательно — день недели в виде числа (1-7, где 1 — воскресенье). Можно указать несколько значений, разделив их запятыми.

## Реальное время: год (`fancymenu_visibility_requirement_is_realtime_year`)

**Назначение:** Проверяет, совпадает ли текущий год по реальному времени с определённым значением.

**Значение:** Обязательно — полный год (например, "2023"). Можно указать несколько значений, разделив их запятыми.

## Файл/папка существует (`fancymenu_loading_requirement_file_exists`)

**Назначение:** Проверяет, существует ли файл или каталог.

**Значение:** Обязательно — путь относительно активной папки игры или путь, начинающийся с `.minecraft/` для стандартной папки Minecraft. Существование файлов и папок учитывается одинаково.

## ОС Linux (`fancymenu_loading_requirement_is_os_linux`)

**Назначение:** Проверяет, что текущая платформа не Windows и не macOS. Обычно это соответствует среде Linux.

**Значение:** Не требуется

## ОС macOS (`fancymenu_loading_requirement_is_os_macos`)

**Назначение:** Проверяет, является ли операционная система macOS.

**Значение:** Не требуется

## ОС Windows (`fancymenu_loading_requirement_is_os_windows`)

**Назначение:** Проверяет, является ли операционная система Windows.

**Значение:** Не требуется

## Есть доступ к интернету (`is_internet_connection_available`)

**Назначение:** Проверяет, доступно ли активное подключение к интернету.

**Значение:** Не требуется

## Язык игры (`fancymenu_loading_requirement_is_language`)

**Назначение:** Проверяет, соответствует ли текущий язык игры определённому значению.

**Значение:** Обязательно — код языка (например, `en_us` для английского)

## Мод загружен (`fancymenu_loading_requirement_is_mod_loaded`)

**Назначение:** Проверяет, загружен ли определённый мод.

**Значение:** Обязательно — ID мода (например, `fancymenu`, `jei`). Также можно проверять OptiFine с помощью `optifine`. Поддерживаются несколько ID модов, разделённых запятыми; все перечисленные моды должны быть загружены.

## Rinku загружен (`is_rinku_loaded`)

**Назначение:** Проверяет, установлен ли и инициализирован ли [Rinku](https://modrinth.com/mod/rinku). [Rinku](https://modrinth.com/mod/rinku) требуется для [элемента Browser](./elements#browser) и [устаревших видео-типов на основе Rinku](./video#requirements); [нативные функции Video](./video) используют Watermedia.

**Значение:** Не требуется

## Число (`fancymenu_visibility_requirement_is_number`)

**Назначение:** Предоставляет расширенное сравнение чисел с разными режимами сравнения.

**Значение:** Обязательно — сложный формат: `["mode":"comparison_mode","number":"value1","compare_with":"value2"]$`, где `comparison_mode` может быть `equals`, `bigger-than`, `smaller-than`, `bigger-than-or-equals` или `smaller-than-or-equals`

## Текст (`fancymenu_visibility_requirement_is_text`)

**Назначение:** Предоставляет расширенное сравнение текста с разными режимами сравнения.

**Значение:** Обязательно — сложный формат: `["mode":"comparison_mode","text":"text1","compare_with":"text2"]$`, где `comparison_mode` может быть `equals`, `contains`, `starts-with` или `ends-with`

## IP сервера (`fancymenu_visibility_requirement_is_server_ip`)

**Назначение:** Проверяет, совпадает ли IP текущего сервера с определённым значением.

**Значение:** Обязательно — IP-адрес сервера (с портом или без него)

## Сервер онлайн (`fancymenu_loading_requirement_is_server_online`)

**Назначение:** Проверяет, онлайн ли и доступен ли определённый сервер.

**Значение:** Обязательно — IP-адрес сервера (с портом или без него)

## Ресурс-пак включён (`is_resource_pack_enabled`)

**Назначение:** Проверяет, выбран ли/активен ли в данный момент определённый ресурс-пак.

**Значение:** Обязательно — название ресурс-пака или ID пакета (например, `Programmer Art` или ID пакета)

## Значение переменной (переменная FM) (`fancymenu_visibility_requirement_is_variable_value`)

**Назначение:** Проверяет, имеет ли переменная FancyMenu определённое значение.

**Значение:** Обязательно — формат: "variable_name:expected_value"

## Только один раз за сессию (`once_per_session`)

**Назначение:** Каждый настроенный экземпляр возвращает true один раз за игровую сессию. Разные экземпляры отслеживаются независимо.

**Значение:** Не требуется
